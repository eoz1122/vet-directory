# Architectural Decisions Log

This document records the major architectural choices, conventions, and security boundaries established for the **EnglishSpeakingVets** (Vet Directory) project.

As per the Global AI Directives, every entry here prevents logic drift and serves as the source of truth for AI agents and human developers. Every entry MUST include an ISO 8601 timestamp in its heading.

---

## 2026-03-18T02:00:00+01:00 — Project Initialization Constraints

**Context:** The application was built as a standalone Vite/React SPA with a small Flask backend for contact form delivery. Security and architectural gaps were identified.

**Decisions:**
1.  **Backend Runtime:** The backend relies on Flask with Gunicorn. It serves exclusively for delivering emails via SMTP.
2.  **No Server-Side State:** The entire data source is a static JSON file (`vets.json`). Data ingestion is handled via separate Python scripts and exported to the web-app. The API does not have database access.
3.  **Client-Side Admin:** The `/admin` route allows in-browser filtering and exports of the `vets.json` file. The local-only basic auth was moved to an environment variable (`VITE_ADMIN_CODE`) to prevent shipping the password in source control.
4.  **Security Boundaries:**
    *   **Nginx:** Reverse proxies to Gunicorn. Blocks unencrypted traffic.
    *   **API:** Flask handles input validation, XSS sanitization, CORS origin restrictions, and rate-limiting (`flask-limiter`, 5 req/min on `/api/contact`).
    *   **System:** The `vet-api.service` runs as a dedicated non-root user (`englishspeaking:www-data`) with strict `systemd` isolation (`NoNewPrivileges`, `ProtectSystem=strict`, `PrivateTmp`).

**Trade-offs:** Client-side admin with an env-var password is not fully secure for a public environment, but it suffices since the admin portal requires no backend write access and relies merely on manual JSON exports.

---

## 2026-03-18T02:30:00+01:00 — Testing Framework Selection

**Context:** The codebase lacked any automated testing. We need a boring, stable, and fast testing setup to adhere to the TDD Iron Law.

**Decisions:**
1.  **Backend Testing:** Use `pytest` for the Python API, specifically testing the input validation logic and rate limiting handling.
2.  **Frontend Testing:** Use `vitest` + `jsdom` + `@testing-library/react` for the React application. Vitest is the native choice for Vite-based projects and provides seamless ESM support.
3.  **TDD Policy:** No production logic changes (especially to critical modules like distance calculations or API validation) without writing failing tests first.

---

## 2026-06-21T17:30:00+02:00 — URL Canonicalization: No Trailing Slash

**Context:** Google Search Console showed 50 pages as "Page with redirect" + 15 as "Alternative page with proper canonical" (≈68 of 115 not-indexed URLs). Root cause: the sitemap and all 35 `<link rel="canonical">` tags use the no-trailing-slash form (`/vets/berlin`), but nginx 301-redirected those to `/vets/berlin/` because the prerendered output is a directory (`/vets/berlin/index.html`), and nginx auto-appends a slash when `try_files` matches a directory before a file.

**Decision:** Standardize on the **no-trailing-slash** canonical form (the form the entire codebase already uses). Fix at the server, not in 60+ files: prepend `$uri/index.html` to nginx `try_files` so the prerendered file is matched first and served with HTTP 200, eliminating the directory trailing-slash redirect. Live change (in `/etc/nginx/sites-available/englishspeakinggermany.online`, web root `/home/englishspeaking/englishspeakinggermany.online`):
`try_files $uri $uri/ /index.html;` → `try_files $uri/index.html $uri $uri/ /index.html;`
`$uri` is kept in the list because this live server block has no separate static-asset `location`, so JS/CSS also resolve through this directive.

**Trade-offs:** Both `/vets/berlin` and `/vets/berlin/` now return 200, but the canonical tag and sitemap point only to the no-slash form, so Google consolidates correctly. Rejected the alternative (rewrite every canonical + sitemap loc + og:url to trailing slash) as a 60-file change that would chase a server quirk instead of fixing it.

**Verification:** `web-app/scripts/seo-smoke-test.sh` asserts canonical URLs return 200 (not 3xx). Confirmed RED pre-fix (6/7 paths 301), GREEN post-fix (7/7 = 200) on 2026-06-21. Asset load + per-page prerendered titles re-confirmed.

**Applied:** Live on 2026-06-21 via `nginx -t && systemctl reload nginx` (graceful, zero downtime). The deploy pipeline (`deploy.sh`) does NOT manage nginx config, so this was a manual server edit; the repo's `web-app/nginx_site.conf` is a stale reference and does not match the live config (different root, no asset block).

**Rollback:** `cp /etc/nginx/sites-available/englishspeakinggermany.online.bak-20260621 /etc/nginx/sites-available/englishspeakinggermany.online && nginx -t && systemctl reload nginx`.

---

## 2026-06-21T21:00:00+02:00 — Single Slug Contract (city/district URLs)

**Context:** A full live crawl of all 211 sitemap URLs found ~9 pages broken + 25 canonical mismatches. Root cause was three divergent slug implementations plus lossy reverse-slug matching:
- `DistrictVets` matched by reversing the slug (`hyphens -> spaces`), which cannot undo `/`, `()`, `&`. Districts like "Friedrichshain / Others" produced a 3-segment path that matched no route -> homepage fallback (6 pages, no H1, ~47 words).
- `CityVets` compared the hyphenated slug directly to `vet.city.toLowerCase()` (spaces) -> every multi-word city ("Bad Homburg") rendered the noindex "City Not Found" page (3 pages).
- The sitemap percent-encoded unicode (`/vets/k%C3%B6nigstein`) while canonicals used raw UTF-8 (`/vets/königstein`) -> ~25 "Alternative page with proper canonical" flags.

**Decision:** One canonical `slugify()` in `src/utils/url.ts` is the single source of truth. Matching is always `slugify(candidate) === param` — never reverse-slug. The two build scripts (`generate-sitemap.js`, `prerender.js`) mirror the exact algorithm; `src/utils/url.test.ts` locks the contract. Display names are taken from the real data (`vet.city`/`vet.district`), falling back to `titleCaseSlug()`. Sitemap now emits raw UTF-8 so `<loc>` equals `<link rel="canonical">` byte-for-byte.

**Trade-offs:** Slug logic is duplicated across the TS app and the plain-JS build scripts (tsconfig has no `allowJs`, and node cannot import `.ts`). Accepted because the contract test + end-to-end crawl guard against drift. The alternative (a shared JS module imported by both) was rejected as more fragile under the current bundler/TS setup.

**Verification:** vitest 12/12, `tsc -b` clean. Post-deploy crawl confirmed the 6 districts + 3 cities now return 200 with unique titles/H1/~290-470 words, and a previously-working page was unchanged (no regression). Ships through the normal git -> deploy pipeline (no manual step).

---

## 2026-06-22T22:50:00+02:00 — Deploy model: GitHub-push-SSH -> VPS pull poller

**Context:** The push -> GitHub Action -> `appleboy/ssh-action` deploy failed repeatedly. Diagnosis from VPS logs: GitHub-hosted runner SYN packets to the VPS never reach sshd (no auth log, no UFW entry) and the action reports `dial tcp: i/o timeout`, while SSH from a normal IP and the VPS's own outbound traffic both work fine. Conclusion: inbound GitHub-runner -> VPS:22 is dropped at the **provider network edge** (not fixable from inside the box). Earlier this also manifested as silent staleness (a push "succeeds" but the red Action means the site never updated) and as `npm ci` races (a manual deploy overlapping an Action wiped node_modules -> `tsc: not found`).

**Decision:** Invert the deploy to **pull-based**, since outbound VPS -> GitHub is reliable.
- VPS systemd timer `esv-deploy.timer` runs `/usr/local/bin/esv-poll-deploy.sh` every 2 min; it `git fetch`es origin/main and runs `deploy.sh` only on change.
- `deploy.sh` hardened: `flock` serializes deploys (no more `npm ci` races) and a post-deploy `curl` liveness check fails loud.
- Host one-time setup: `git config --system --add safe.directory <repo>` (service runs as root; repo owned by `englishspeaking`) and `Environment=HOME=/root` in the unit.
- The GitHub Action is reduced to a manual-only no-op so pushes stop failing on the dead SSH step.
- Reference copies of the units/script live in `deploy/`.

**Trade-offs:** Up to ~2 min latency between push and deploy (fine; the prerender already takes ~3 min). Deploy now depends on the VPS timer rather than CI; mitigated by the units being version-controlled in `deploy/` for quick rebuild.

**Verification:** The timer/service deployed `4bd4a64` end-to-end (the commit the broken Action never shipped); server HEAD confirmed. `deploy.sh` liveness check passed.

**Rollback:** `systemctl disable --now esv-deploy.timer` reverts to manual `bash deploy.sh`; the old push->SSH workflow remains in git history.

---

## 2026-06-23T03:15:00+02:00 — Community "confirm English-speaking" (email relay, no DB)

**Context:** Wanted a positive community-verification signal (the directory is community-sourced) without adding a database, staying consistent with the "no server-side state" decision. At current traffic, live confirm-counts would mostly read "0", so deferred storage.

**Decision:** One-click "Confirm they speak English" button on vet cards posts to a new `POST /api/confirm-vet` (Flask), which validates + sanitizes + honeypots + rate-limits (10/min) and emails the admin; the admin then bumps `community_status`/`last_scanned` in `vets.json`. Mirrors the existing `submit_vet`/`report_issue` email-relay pattern. Added a `company` honeypot to the contact form too, and a shared `send_email` helper (DRY). TDD: 11 backend tests.

**Deploy note (gotcha):** `deploy.sh` now `systemctl restart vet-api` so backend code changes take effect (gunicorn does not auto-reload). BUT `deploy.sh` git-resets itself at step 1, so a change *to deploy.sh* lands a deploy late (bash already buffered the old script). The restart line therefore first took effect one deploy after it was added; the initial rollout needed a manual `systemctl restart vet-api`.

**Trade-offs:** No live social-proof counts (Option B / SQLite deferred until traffic justifies it). Anonymous confirmations are spammable; mitigated by rate-limit + honeypot, and they only *notify* the admin (no automatic data change).

**Verification:** Backend pytest 11/11; live endpoint returns 400 on bad input and 200 on honeypot (no email); button renders on prerendered city/district pages.

---

## 2026-06-23T00:30:00+02:00 — Evergreen blog URLs (drop -2025) + 301 redirects

**Context:** 12 blog posts had year-stamped slugs (`/blog/...-2025`) that read as permanently dated. The April SEO audit recommended evergreen URLs.

**Decision:** Rename all 12 to drop `-2025` and 301-redirect the old URLs so indexed equity and inbound links survive. Updated in lockstep so sitemap loc = `<link rel="canonical">` = `og:url` = Article schema url = App.tsx route = internal `<Link>` refs (the same agreement that the slug-contract fix established for city/district pages — a mismatch recreates the "Page with redirect" / "Alternative page" indexing failures). Redirects are exact-match nginx rules (`location = /blog/<old> { return 301 .../<new>; }`) placed before `location /`, so they intercept before try_files. Kept the no-trailing-slash convention.

**Trade-offs:** Redirects live only in the hand-maintained nginx config (not deploy-managed), so a VPS rebuild must reapply them from `web-app/nginx_site.conf`. `datePublished` left at the real historical date.

**Verification:** `web-app/scripts/seo-smoke-test.sh` extended — new URLs must 200, all 12 retired URLs must 301 to the evergreen target. Confirmed PASS live. Orphaned `-2025` prerendered dirs removed from the web root.

**Follow-up:** Resubmit the sitemap in Google Search Console so the new URLs are recrawled and the redirects validated.

## 2026-07-10T14:00:00+02:00 — IndexNow for Bing/Yandex Recrawl

**Context:** Google indexing of the directory's long tail is throttled by processing lag and thin-page authority, which more code cannot fix. IndexNow is a free, legitimate lever for the non-Google engines (Bing, Yandex, Seznam) that honor push notifications.

**Decisions:**
1.  **Key file:** `web-app/public/fcf5c3b58393e18fa2aa5f59fede4a13.txt` (bare 32-hex key, no newline). Ships in the build to the web root and is served at `https://englishspeakinggermany.online/<key>.txt` as IndexNow's ownership proof. Public by design, not a secret.
2.  **Submit script:** `web-app/scripts/indexnow-submit.js` auto-detects the key file, reads `public/sitemap.xml`, and bulk-POSTs all URLs to `api.indexnow.org/indexnow`. Non-fatal: logs and exits 0 on any API error.
3.  **Deploy hook:** `deploy.sh` step 8 runs the submit after the liveness check, so every deploy nudges the non-Google engines automatically.

**Trade-offs:** Google ignores IndexNow entirely, so this does not move the primary GSC numbers; it only serves Bing/Yandex traffic (~3-5% of search). First submission returns `403 SiteVerificationNotCompleted` until the engine fetches the key file async; it self-heals on retry (confirmed HTTP 200 for 222 URLs on 2026-07-10).

## 2026-07-11T02:20:00+02:00 — End-to-End Audit: Data Honesty, 404s, and Hardening

**Context:** A six-dimension audit (live infra, security, frontend/SEO, data integrity, dependencies, tests) found user-facing data bugs, on-site SEO defects feeding the GSC "not indexed" pile, and security gaps. Fixes were approved in four waves.

**Decisions:**
1.  **Display filter over deletion for closed practices:** `src/utils/activeVets.ts` (`filterDisplayableVets`) is the single source of truth. Records marked `Permanently Closed` stay in `vets.json` as an audit trail but never render, and never create sitemap/prerender routes (both scripts mirror the filter). True duplicates, by contrast, are deleted outright (Internal-3, Nuremberg-New-4; 252 -> 250).
2.  **Real 404s replace soft-404s:** every legitimate route is prerendered, so nginx `try_files` now ends in `=404` with `error_page 404 /404/index.html` (a prerendered, noindexed NotFound page). `/admin` is the only SPA-shell exception (`location = /admin`). Trailing-slash URLs 301 to the canonical no-slash form.
3.  **Guides must live in Blog.tsx to exist:** the sitemap and prerender harvest routes from Blog.tsx `url:` fields; the Munich/Hamburg/Frankfurt emergency guides were routed but invisible to crawlers. Rule: any new content page MUST be added to Blog.tsx `blogPosts` (or the scripts' static lists).
4.  **District pages get inbound links:** CityVets renders a "Browse by district" index plus linked district chips, ending the orphan-page pattern (129 district pages had zero internal inbound links).
5.  **Secrets:** the hardcoded Google API key in `scripts/maintenance/fix_addresses.py` was removed (argv pattern, matching `geocode_missing.py`). The key itself must be rotated by the owner - removal from HEAD does not purge git history in the public repo.
6.  **Least-privilege deploy:** `esv-deploy.service` runs as `englishspeaking` (npm lifecycle scripts no longer execute as root); `vet-api` restart is delegated via a narrow sudoers rule (`NOPASSWD: /usr/bin/systemctl restart vet-api`).
7.  **HSTS with training wheels:** enabled at `max-age=86400` (1 day) for easy rollback; bump to 31536000 after a quiet week. Permissions-Policy allows `geolocation=(self)` because the "near me" search uses it. CSP deferred: needs testing against Google Maps/GA before enforcement.

**Rollback:** wave commits are atomic and revertable individually; nginx changes are manual on the VPS (previous conf retained as a dated .bak before edit; `sudo nginx -t` gate before reload).

## 2026-07-21T17:37:03+02:00 - Proxy-aware API limits and bounded SMTP

**Context:** The Flask limiter used `request.remote_addr` behind Nginx without trusted-proxy middleware. Gunicorn therefore identified requests by the loopback proxy address, while its in-memory counters remained isolated across three workers. SMTP connections also had no timeout, so a slow mail server could occupy every synchronous API worker.

**Decision:** Nginx is the primary shared rate-limit layer for the two public write endpoints. Two `limit_req_zone` budgets use `$binary_remote_addr`: 5 requests per minute for `/api/contact` and 10 per minute for `/api/confirm-vet`, with bounded bursts and HTTP 429 responses. Flask keeps its existing limits as defense in depth. `ProxyFix` trusts exactly one `X-Forwarded-For` hop and no other forwarded headers; this is safe only while Gunicorn remains bound to `127.0.0.1` behind one Nginx proxy. SMTP connections use a configurable `SMTP_TIMEOUT_SECONDS`, defaulting to 10 seconds.

**Trade-offs:** Nginx configuration still requires a manual VPS update and graceful reload. The Flask in-memory limiter remains worker-local, but it is now secondary to the shared Nginx zones. Only the forwarded client address is trusted because the API does not need forwarded host, port, prefix, or protocol data.

**Verification:** TDD RED confirmed three failures for missing proxy trust, SMTP timeout, and Nginx endpoint limits. GREEN passed all 16 API tests, all 52 frontend tests, Python compilation, and TypeScript. The local workstation has no Nginx binary, so final syntax validation must run on the VPS before reload.

**Rollback:** Restore the previous `api/main.py`, `api/nginx_vet_api.conf`, and `web-app/nginx_site.conf`; run `sudo nginx -t`; then reload Nginx and restart `vet-api`. If only Nginx enforcement causes trouble, remove the two exact endpoint locations and zone declarations while leaving Flask limits active.

## 2026-07-21T17:48:20+02:00 - Python dependency vulnerability remediation

**Context:** `pip-audit` found five advisories in the runtime pins for Flask, Flask-CORS, and python-dotenv. After those fixes, a complete scan of the frozen development environment found three more advisories in Click, Pygments, and pytest.

**Decision:** Upgrade to the smallest patched compatible releases where possible: Flask 3.1.3, python-dotenv 1.2.2, Click 8.3.3, Pygments 2.20.0, and pytest 9.0.3. Flask-CORS moves to the current 6.0.5 release because the 5.x line contains the reported path-matching vulnerabilities. Click is pinned in runtime requirements as well as development requirements so an existing VPS virtual environment cannot retain the vulnerable transitive version during a normal non-upgrade install.

**Trade-offs:** Flask-CORS crosses a major version, but the project uses only the stable global `CORS(app, origins=...)` interface and its API tests remain green. Requirement files still lack hashes, so artifact integrity is not locked cryptographically; introducing a compiled hash lock is a separate supply-chain change.

**Verification:** PyPI metadata confirmed Python compatibility. `pip check` found no broken requirements, all 16 API tests passed, and `pip-audit` reported no known vulnerabilities in either `requirements.txt` or the complete `requirements-dev.txt` set.

**Rollback:** Restore the prior pins and reinstall `api/requirements-dev.txt`. Rollback reintroduces known vulnerabilities and is acceptable only as a short emergency measure while isolating the API and preparing a compatible patched release.

## 2026-07-21T17:58:34+02:00 - Public repository artifact boundary

**Context:** The public repository still tracked an internal CSV export, a local SQLite processing database, operating-system metadata, Python bytecode, and temporary debug or resolved-document files. Several were already listed in `.gitignore`, but ignore rules do not affect files previously committed.

**Decision:** Keep source code and intentional source datasets under version control, but remove local runtime and generated artifacts from the current repository state. `web-app/vets-database-internal.csv` and `data/processing/vets_berlin_potential.db` remain available locally and are ignored because maintenance scripts still consume them. `.DS_Store`, Python bytecode, debug output, and resolved README artifacts are deleted as disposable files. Root ignore rules now cover Python virtual environments, bytecode, pytest state, and local processing databases.

**Trade-offs:** A fresh clone will not contain the internal CSV or processing database. Operators must obtain or regenerate them through the approved internal data workflow. This cleanup affects the current branch only; the public Git history still contains earlier versions and would require a separately approved history rewrite and coordinated force push to remove.

**Verification:** SHA-256 checksums confirmed the preserved CSV and database were unchanged after untracking. `git check-ignore` covers every preserved or reproducible target, and `git ls-files` reports no remaining candidates from the approved artifact set.

**Rollback:** Remove the corresponding ignore rules and use `git add -f` to track the preserved CSV or database again. Deleted disposable artifacts can be restored from the previous commit, although regenerating them is preferred.

## 2026-07-21T18:09:38+02:00 - Report-only Content Security Policy rollout

**Context:** The site had no Content Security Policy because Google Maps, Google Analytics, Google Fonts, and inline JSON-LD or vendor-injected styles require a tested allowlist. Enforcing an untested policy could disable maps, analytics, structured data, or normal page rendering.

**Decision:** Add `Content-Security-Policy-Report-Only` to the production Nginx configuration and the local Vite server. The policy defaults resources to the same origin, blocks plugins, restricts framing and form targets, and explicitly allows the Google services used by the application. Inline script and style remain temporarily allowed while analytics, JSON-LD, and Maps integrations are migrated or proven compatible with nonces or hashes. Browser violations are sent to `/api/csp-report`. That endpoint accepts at most 16 KiB, is rate-limited in both Nginx and Flask, removes query strings and fragments from reported URLs, strips log-control characters, and records only bounded fields.

**Trade-offs:** Report-only mode observes violations but does not block malicious or unexpected resources. The legacy `report-uri` directive is used for broad browser compatibility; a future `Reporting-Endpoints` and `report-to` migration can supplement it. Local development adds only the connections Vite needs and does not add `unsafe-eval` to the production policy.

**Verification:** TDD RED confirmed missing report handling, bounds, and policy configuration. GREEN passed all 20 API tests, all 52 frontend tests, ESLint, and TypeScript. A loopback browser check loaded the home page, Google map, contact form, and Berlin directory with no CSP errors or failed images. The only console warning was Google's separate recommendation to migrate from its legacy Places Autocomplete API. The local report endpoint returned 204 and logged a sanitized sample without its query string.

**Production rollout:** Back up the active Nginx configuration, apply the configuration snapshots, run `sudo nginx -t`, and reload only after syntax validation succeeds. Keep the policy in report-only mode while reviewing violation logs under real traffic. Enforce it only after legitimate sources are covered and the violation stream is quiet.

**Rollback:** Remove the `Content-Security-Policy-Report-Only` header, the CSP report rate-limit zone, and the exact `/api/csp-report` Nginx location; validate with `sudo nginx -t` and reload Nginx. The Flask endpoint can remain dormant or be reverted with `api/main.py` after restarting `vet-api`.

## 2026-07-21T18:15:06+02:00 - Single-source SPA page-view tracking

**Context:** The Google tag configuration sent an automatic page view on initial load while `GoogleAnalyticsTracker` issued another `config` command on mount and on every React Router navigation. This could duplicate the initial view, and repeated `config` calls conflict with current Google tag guidance.

**Decision:** Initialize the Google tag once with `send_page_view: false`. React Router is the sole code-level source of page views and sends one explicit `page_view` event for each pathname or query-string change. The event is deferred by one task so route metadata can update, and its cleanup cancels React Strict Mode's disposable first effect pass. Hash-only changes are not treated as new pages.

**Trade-offs:** GA4 Enhanced Measurement can independently send page views for browser-history changes even when `send_page_view` is false. The Google Analytics web-stream setting named "Page changes based on browser history events" must be disabled to guarantee single-source tracking. That account-level change is outside this repository and was not performed.

**Verification:** TDD RED demonstrated the automatic initial view and repeated route `config` behavior. GREEN passed 54 frontend tests across 10 files, ESLint, and TypeScript. The tracker test verifies one initial event and one event after a client-side navigation.

**Rollback:** Restore `send_page_view: true` in `web-app/index.html` and restore the route tracker's prior `config` call. This reintroduces the duplicate-counting risk and should only be used if manual page-view events stop reaching GA4.

## 2026-07-21T20:04:04+02:00 - GA4 and Search Console measurement cleanup

**Context:** The authenticated GA4 audit confirmed that Enhanced Measurement still emitted page views for browser-history changes, which would duplicate the repository's explicit React Router page views. The property also had no meaningful outbound conversion configured. Search Console had last read a 211-URL sitemap on 21 June even though the live sitemap now contains 231 URLs, and its three redirect-error examples had not been crawled since 11 April.

**Decision:** Disable only the GA4 Enhanced Measurement option named "Page changes based on browser history events" while leaving the other enhanced measurements active. Mark `vet_website_click` as a GA4 key event because sending a visitor to a veterinary practice is the directory's primary conversion. Resubmit `/sitemap.xml` in Search Console and start validation for the three historical redirect errors after verifying their live behavior.

**Verification:** GA4 was reopened after saving and the browser-history checkbox remained unchecked. The Events screen showed `vet_website_click` as pressed and confirmed that it was enabled as a key event. Search Console confirmed successful sitemap submission on 21 July 2026 and changed redirect validation to "Validation Started" with the same date.

**Deferred:** The obsolete misspelled `conract_form_filled` key event remains unchanged until the application sends a distinct `contact_form_success` event. Event retention remains at two months because increasing it to fourteen months is a separate privacy and analytics-retention decision.

**Rollback:** Re-enable browser-history page views in the web stream's Enhanced Measurement advanced settings and save, or unstar `vet_website_click` on the GA4 Events screen. Sitemap resubmission is harmless and needs no rollback. Search Console validation can be allowed to finish; it does not alter website behavior or indexing directives.

## 2026-07-21T20:06:53+02:00 - Dedicated successful-contact conversion event

**Context:** The contact page emitted the generic GA4 event name `form_submit` only after the API succeeded. GA4 Enhanced Measurement also emits `form_submit` for form interactions, so reports could not reliably distinguish attempted submissions from contacts actually accepted by the backend. The property still contains an obsolete misspelled key event named `conract_form_filled`.

**Decision:** Emit `contact_form_success` only after a successful `/api/contact` response. Keep the topic and non-personal classification parameters, and never include the visitor's name, email, message, or submitted practice details in analytics.

**Verification:** TDD RED showed the successful request emitted `form_submit`. GREEN verifies that a successful response emits exactly one `contact_form_success` event and that a rejected response emits no conversion. All 56 frontend tests passed across 11 files, with ESLint and TypeScript also clean.

**Follow-up:** After this frontend change is deployed and the first real event reaches GA4, mark `contact_form_success` as a key event and unmark the obsolete `conract_form_filled` event. Do not mark the generic Enhanced Measurement `form_submit` event as a conversion.

**Rollback:** Restore the previous event name in `web-app/src/pages/Contact.tsx`. This would again mix successful contacts with GA4's automatic form events and is not recommended.

## 2026-07-21T20:10:52+02:00 - Retire sitelinks SearchAction structured data

**Context:** Search Console reports two literal search-template URLs as soft 404s: `?s={search_term_string}` and `?q={search_term_string}`. The homepage does implement query-parameter filtering, so the search action was not fictitious. However, Google retired the sitelinks search box globally in November 2024 and no longer uses its `SearchAction` markup for a search result feature.

**Decision:** Remove only `potentialAction` from the homepage `WebSite` JSON-LD. Retain the supported `WebSite` type, site name, and canonical site URL. The visible homepage search and its `s` and `q` query-parameter behavior remain unchanged.

**Trade-offs:** The soft 404 entries are harmless exclusions rather than ranking penalties, and Google states that leaving the unsupported markup would not itself cause Search problems. Removal is still preferred because the markup has no remaining Google feature benefit and continues to advertise crawler-discoverable template URLs.

**Verification:** TDD RED captured the emitted `SearchAction`. GREEN verifies the rendered JSON-LD contains only the intended `WebSite` identity fields. All 57 frontend tests passed across 12 files, with ESLint and TypeScript also clean.

**Follow-up:** After deployment, allow Google to recrawl the homepage and then start Search Console validation for the two soft 404 examples. Historical exclusions can remain visible until Google's next processing cycle.

**Rollback:** Restore the `potentialAction` object in `web-app/src/pages/Home.tsx`. This restores unsupported sitelinks search box markup and is not expected to improve rankings or appearance in Google Search.

## 2026-07-21T20:14:26+02:00 - Canonicalize legacy slash-bearing district URLs

**Context:** The sitemap generator used before the shared `slugify()` contract preserved `/` characters inside six district labels. Google consequently discovered malformed nested paths such as `/vets/berlin/düppel/zehlendorf` and `/vets/hamburg/bahrenfeld/altona`. Those paths are not canonical district routes and can create duplicate or soft-indexing noise when a generic page handles them.

**Decision:** Add exact permanent redirects for all six historical path shapes to their normalized district slugs. Define both trailing and non-trailing variants so every request reaches its canonical URL in one hop. Keep the rules exact instead of applying a broad `/vets/` rewrite, which protects legitimate city and district routes from unintended transformations.

**Trade-offs:** The Nginx snapshot gains twelve explicit rules, but the finite list is safer and easier to audit than reconstructing obsolete slug behavior at request time. Literal UTF-8 is retained for `düppel`, matching Nginx's normalized URI matching and the repository's UTF-8 canonical URL contract.

**Verification:** TDD RED confirmed the redirect contract was absent. Targeted GREEN passed for all twelve exact locations and confirmed that the sitemap contains each normalized canonical URL while excluding its legacy nested form. The complete verification gates then passed: 21 API tests, 57 frontend tests, ESLint, and TypeScript.

**Production rollout:** Apply the updated site configuration, run `sudo nginx -t`, and reload only after syntax validation succeeds. Then verify every legacy URL returns one 301 directly to a 200 canonical page before starting Search Console validation. No deployment was performed during this change.

**Rollback:** Remove the twelve legacy district `location` entries, run `sudo nginx -t`, and reload Nginx. Canonical pages and the sitemap remain unaffected, but historical malformed URLs will again fall through to normal unknown-path handling.

## 2026-07-21T20:23:18+02:00 - Keep clinic report details out of crawlable URLs

**Context:** Search Console's 57 "Crawled - currently not indexed" examples included seven internally generated `/contact` URLs containing clinic IDs, clinic names, and report reasons in query parameters. The contact page already canonicalized those variants to `/contact`, so non-indexing was correct, but the links created unnecessary crawl variants and placed operational details in URLs and same-origin request logs.

**Decision:** Render every clinic-report action with the clean `/contact` URL and pass its transient prefill through React Router navigation state. Centralize the behavior in `ReportIssueLink` so the homepage, city pages, and district pages cannot drift. Validate navigation state at runtime with bounded strings before using it. Continue accepting the historical query format so bookmarks and URLs already known to Google still prefill correctly.

**Trade-offs:** Navigation state is intentionally transient. Copying or reloading `/contact` does not retain a newly selected clinic report, while legacy query URLs still work. This is preferable to publishing clinic names and IDs in crawlable URLs. The remaining crawled-not-indexed examples include canonical content pages and historical trailing-slash variants, so they should not be mass-noindexed solely to reduce the report count.

**Verification:** TDD RED confirmed that report links exposed their details in `href` values and that `Contact` ignored router state. GREEN verifies a clean `/contact` link, correct state transfer, state-based form prefill, and legacy query compatibility. Repository searches find no production clinic-report query links. All 21 API tests and 60 frontend tests pass, with ESLint, TypeScript, and `git diff --check` also clean.

**Follow-up:** After deployment and recrawl, the query examples should age out while resolving through the existing `/contact` canonical. Do not start validation for the entire crawled-not-indexed category until the remaining canonical pages have been evaluated individually.

**Rollback:** Replace the three `ReportIssueLink` call sites with their former query-string `Link` values and remove the router-state branch from `Contact.tsx`. This restores shareable prefilled URLs but also recreates crawl variants and URL-level data exposure.

## 2026-07-21T20:30:59+02:00 - Consolidate Hamburg emergency-vet search intent

**Context:** Two routes targeted the same Hamburg emergency-vet query: `/guides/emergency-vets-hamburg` and `/blog/emergency-vet-hamburg-english`. Search Console listed the blog article as crawled but not indexed and separately reported a 65 percent impression decline. The blog article is the stronger asset because it is newer, substantially more complete, and already has historical search impressions. Live verification also found that the `/guides/` URL currently returns a generic homepage render with the homepage canonical, while the blog URL returns its intended article and self-canonical.

**Decision:** Make `/blog/emergency-vet-hamburg-english` the only indexable Hamburg emergency guide. Remove the older thin `PetEmergencyHamburg` component and its duplicate blog card and sitemap entry. Point all internal Hamburg emergency links to the canonical article. Permanently redirect both trailing variants of `/guides/emergency-vets-hamburg` directly to the canonical URL in Nginx, while retaining a React `Navigate` fallback for client-side navigation that bypasses the server redirect.

**Trade-offs:** The retired guide's shorter presentation is removed rather than merged because the canonical article already contains its only unique clinic details plus broader emergency guidance. A React fallback keeps local and client-side navigation resilient, but production canonicalization depends on applying and validating the Nginx configuration. The consolidation follows Google's recommended combination of permanent redirect, consistent internal links, self-canonical metadata, and sitemap inclusion.

**Verification:** TDD RED confirmed two independent sitemap entries, duplicate application components, stale internal links, and missing permanent redirects. GREEN verifies the exact redirect pair, one canonical sitemap entry, one blog card, canonical internal links, removal of the obsolete component, and the React fallback. All 22 API tests and 60 frontend tests pass, with ESLint, TypeScript, `git diff --check`, and artifact cleanup also clean.

**Production rollout:** Apply the updated site and Nginx configuration, run `sudo nginx -t`, and reload only after syntax validation succeeds. Verify `/guides/emergency-vets-hamburg` and its trailing variant each return one 301 to a 200 `/blog/emergency-vet-hamburg-english` response with a self-referential canonical. No deployment was performed during this change.

**Follow-up:** After deployment, request reindexing for the canonical blog article through URL Inspection. Do not validate the entire crawled-not-indexed category because it contains unrelated pages with different causes.

**Rollback:** Restore `PetEmergencyHamburg.tsx`, its lazy import and route, its blog card and sitemap entry, and the two former internal links; remove the Nginx redirect pair. Run `sudo nginx -t` before reloading. This rollback recreates the overlapping search intent and is not recommended except to recover missing content.

## 2026-07-21T20:34:47+02:00 - Consolidate Berlin emergency-vet search intent

**Context:** Berlin had two pages targeting the same emergency-vet query: `/guides/emergency-vets-berlin` and `/blog/emergency-vet-berlin-english`. Both live URLs currently return the homepage render and homepage canonical because their prerendered route artifacts have not been deployed. Search Console still distinguishes their historical signals: over the last three months the guide URL received 41 impressions at average position 53.4, while the blog URL received 12 impressions at average position 40.1; neither received clicks. The blog component contains the newer and more complete emergency article, but the guide URL has the stronger established discovery signal and already owns most internal links.

**Decision:** Keep `/guides/emergency-vets-berlin` as the sole canonical URL while serving it with the richer `EmergencyVetBerlinGuide` content. Remove the older thin `EmergencyVetsBerlin` component and the duplicate blog card and sitemap entry. Update the richer article's canonical, Open Graph URL, Article schema URL, and related-post identity to the guide URL. Permanently redirect both trailing variants of `/blog/emergency-vet-berlin-english` to the guide in Nginx, with a React `Navigate` fallback for client-side navigation.

**Trade-offs:** The canonical URL does not match the retained component filename, but keeping that filename avoids a large content-file rename while its exported component remains clear. The guide URL's historical position was weaker than the blog URL's, but it had more than three times the impressions and stronger internal linking. Consolidating those signals onto the richer content is preferable to maintaining two overlapping assets.

**Verification:** TDD RED confirmed two sitemap entries, two application components, conflicting self-canonicals, and missing redirects. GREEN verifies one guide sitemap and blog-card URL, canonical metadata throughout the richer article, two direct 301 rules, the React fallback, and removal of the old component. All 23 API tests and 60 frontend tests pass, with ESLint, TypeScript, `git diff --check`, and artifact cleanup also clean.

**Production rollout:** Apply the built site and Nginx configuration, run `sudo nginx -t`, and reload only after syntax validation succeeds. Verify both blog URL variants return one 301 to a 200 guide page whose rendered canonical is `https://englishspeakinggermany.online/guides/emergency-vets-berlin`. No deployment was performed during this change.

**Follow-up:** After deployment, request indexing for the guide URL in Search Console. Monitor the retired blog URL until Google transfers its signals and reports it as redirected or an alternate rather than a separate content page.

**Rollback:** Restore `EmergencyVetsBerlin.tsx`, its lazy import and guide route, restore the blog route and card, restore the blog sitemap entry and the richer article's blog canonicals, and remove the two Nginx redirects. Validate Nginx before reload. This recreates the competing search targets.

## 2026-07-21T20:38:38+02:00 - Make retired content redirects direct and symmetric

**Context:** Search Console's nine "Duplicate without user-selected canonical" examples contained five malformed district URLs already covered by the new district rules and four retired blog URLs. Live checks showed that three trailing-slash blog URLs required two redirects before reaching their canonical pages. The fourth URL, `/blog/public-transport-dogs-frankfurt-rmv`, returned 404 and was still linked from the Cologne transport article. Source inspection found three more broken sibling aliases for Hamburg, Munich, and Stuttgart in the same article.

**Decision:** Give every retired blog URL an exact trailing-slash twin that returns one 301 directly to the canonical destination. Add exact redirect pairs for all four obsolete transport aliases and update the Cologne article to link directly to the canonical city guides. Add the trailing variant for the retired Nuremberg district because it shares the deployment smoke-test contract. Expand the SEO smoke test so every listed retirement is checked with and without a trailing slash, including the recent Hamburg and Berlin emergency-guide consolidations.

**Trade-offs:** The Nginx configuration gains explicit rules instead of relying on the generic trailing-slash rewrite. This is more verbose, but it prevents redirect chains and makes every historical mapping independently auditable. Redirects remain limited to known obsolete paths, so valid routes are unaffected.

**Verification:** TDD RED failed on the first missing trailing twin. GREEN verifies symmetry for every exact blog redirect, all four transport alias mappings, removal of obsolete production links, and coverage in the deployment smoke test. Live pre-change checks recorded two redirects for the three reported trailing URLs and a direct 404 for the Frankfurt alias. All 24 API tests and 60 frontend tests pass, with ESLint, TypeScript, shell syntax validation, `git diff --check`, and artifact cleanup also clean.

**Production rollout:** Apply the Nginx configuration, run `sudo nginx -t`, and reload only after syntax validation succeeds. Run `web-app/scripts/seo-smoke-test.sh` after the site build is active; it must report one direct 301 for every retired path variant and 200 for every canonical URL. No deployment was performed during this change.

**Follow-up:** After deployment and smoke-test success, start Search Console validation for "Duplicate without user-selected canonical." The entire nine-URL sample is now covered by either the district redirect set or this retired-content redirect set.

**Rollback:** Restore the four obsolete links in `PublicTransportCologne.tsx`, remove the new alias and trailing redirect entries, and revert the smoke-test loop to one variant. Validate Nginx before reload. This reintroduces broken links and redirect chains and is not recommended.

## 2026-07-21T20:49:22+02:00 - Redirect the remaining historical noindex slug

**Context:** Search Console listed 17 URLs under "Excluded by noindex tag." Live browser checks found that 15 now render active, indexable vet listings and that the report is stale because Google last crawled them between 26 March and 24 May 2026. The remaining two examples are the same historical Frankfurt district path with and without a trailing slash: `/vets/frankfurt/am-nordpark-10,-60437-frankfurt-am-main`. Both render an empty district page, while the normalized comma-free slug is active and present in the sitemap.

**Decision:** Permanently redirect both historical variants directly to `/vets/frankfurt/am-nordpark-10-60437-frankfurt-am-main`. Include the mapping in the deployment SEO smoke test and extend the legacy-district redirect contract so the canonical sitemap inclusion and legacy exclusion remain verified.

**Trade-offs:** Exact Nginx locations add two lines, but they avoid a broad punctuation rewrite that could alter unrelated valid URLs. Search Console validation remains unstarted until the rules are live, because starting it before rollout could fail on the two currently noindexed examples.

**Verification:** TDD RED failed because both exact mappings and the smoke-test case were absent. The targeted GREEN test passed after adding them. Full project verification is recorded with the audit handoff.

**Production rollout:** Apply the Nginx configuration, run `sudo nginx -t`, reload only after syntax validation, then run `web-app/scripts/seo-smoke-test.sh`. No deployment was performed during this change.

**Follow-up:** After the smoke test passes on the live site, start validation for "Excluded by noindex tag" in Search Console. Google should retain the 15 valid listing URLs and recategorize the two retired paths as redirects.

**Rollback:** Remove the two exact Nginx locations and the matching smoke-test entry, then run `sudo nginx -t` before reloading. This restores the empty noindex responses for the historical slug.

## 2026-07-21T20:51:30+02:00 - Keep frontend tests outside the production TypeScript project

**Context:** The full verification gate found that `tsconfig.app.json` excluded `.test.ts` and `.spec.ts` files but not their `.tsx` equivalents. As a result, an existing React analytics test was compiled as production application code and failed on its test-only Node imports, even though the frontend test suite itself passed.

**Decision:** Extend the existing exclusion policy to `.test.tsx` and `.spec.tsx`. Vitest remains responsible for compiling and running test files, while the application TypeScript project validates production source only.

**Trade-offs:** Production type checking no longer checks test files, which matches the existing intent for `.ts` tests. Test syntax and types continue to be checked by Vitest during the test gate.

**Verification:** The TypeScript build check failed before the change with missing `node:fs`, `node:path`, and `process` types from the test file. It passed after the exclusion patterns were made extension-complete. ESLint and all 60 frontend tests also passed.

**Rollback:** Remove the two `.tsx` exclusion patterns. This will restore the production type-check failure unless the test project is separately configured with Node types.

## 2026-07-21T21:00:12+02:00 - Redirect retired search-template placeholders exactly

**Context:** Search Console reports two Soft 404 examples, `/?s={search_term_string}` and `/?q={search_term_string}`, first crawled in February 2026. They are literal placeholders discovered from the retired `SearchAction` schema, not user searches. The schema has already been removed, but the historical URLs still return the valid homepage with HTTP 200. The same audit reviewed all 57 "Crawled - currently not indexed" examples: 25 are trailing-slash aliases covered by the generic redirect, one is a retired blog path with an exact redirect, seven are legacy contact-report queries now absent from internal links and canonicalized to `/contact`, and the remaining canonical content and listing pages are technically indexable. No additional indexing directive is justified for that group.

**Decision:** Add one Nginx request-URI condition that permanently redirects only the literal `s` and `q` placeholder forms, including percent-encoded braces, to the canonical homepage. Preserve every real homepage search query, including `?s=berlin` and `?q=hamburg`. Do not start Search Console validation until the rule is live and verified.

**Trade-offs:** Waiting for recrawl after schema removal would avoid a server rule but could leave the two historical URLs returning misleading 200 responses indefinitely. A 410 would describe the synthetic URLs accurately but would discard their direct relationship to the homepage. The narrow 301 resolves the historical crawl state while avoiding a broad query-string redirect.

**Verification:** TDD RED failed because the placeholder redirect and deployment checks were absent. GREEN verifies that the configured regular expression matches only the literal and encoded placeholder forms, rejects real searches, extra parameters, and non-homepage paths, and that the smoke test checks both redirects plus two valid query passthroughs. All 25 API tests and 60 frontend tests pass. ESLint, TypeScript, and shell syntax checks are clean. A local Nginx binary is unavailable, so production syntax validation remains a mandatory rollout gate.

**Production rollout:** Apply the Nginx configuration, run `sudo nginx -t`, and reload only after syntax validation succeeds. Run `web-app/scripts/seo-smoke-test.sh` against the live site. No deployment was performed during this change.

**Follow-up:** After the live smoke test passes, start Search Console validation for Soft 404. Do not validate "Crawled - currently not indexed" as a fixed technical defect; monitor its valid canonical pages through normal recrawling and content performance instead.

**Rollback:** Remove the placeholder-only `if` block from `web-app/nginx_site.conf` and the corresponding smoke-test arrays and loop, then run `sudo nginx -t` before reloading. The two synthetic URLs will return the homepage with HTTP 200 again.

## 2026-07-21T21:07:23+02:00 - Do not validate intentional redirects as fixed pages

**Context:** Search Console marks the 69-URL "Page with redirect" group as failed validation. The validation ran from 21 June to 1 July 2026 and failed on only `/blog/pet-insurance-germany-2025` and `/blog/pet-friendly-apartments-germany-2025`. Both URLs are intentionally retired and permanently redirect to their evergreen replacements, so remaining in the redirect category is correct behavior rather than a defect.

**Decision:** Do not restart validation for "Page with redirect." Treat this Search Console category as informational for intentional migrations. Continue testing redirect correctness directly: one permanent redirect, a valid final destination, no retired URL in the sitemap, and no current internal link to the retired form.

**Evidence:** All 69 Search Console examples were reviewed. Live HTTP checks show that 45 canonical-looking pages return 200, all 18 contact and query examples return 200, and four retired aliases return one 301 to a destination that returns 200. Two recently retired aliases, `/blog/emergency-vet-berlin-english` and `/vets/berlin/friedrichshain-/-others`, still return 200 on the live site because their exact redirect rules are present only in the current undeployed project changes. They are already covered by the redirect contract and deployment smoke test.

**Trade-offs:** Search Console will continue counting intentional redirects as excluded pages, which is expected and harmless. Restarting validation would create another failure because Google interprets "fixed" as the source URL no longer redirecting. Removing the redirects merely to satisfy the report would revive duplicate or malformed URLs and lose the intended canonical consolidation.

**Follow-up:** After the existing Nginx changes are eventually rolled out, run `web-app/scripts/seo-smoke-test.sh` and monitor the redirect count for unexpected growth. Investigate only new URLs, redirect chains, loops, broken destinations, or canonical URLs that redirect unexpectedly.

**Rollback:** This entry records an operational interpretation and makes no production change. If Google changes the validation semantics, reassess against its current documentation before starting a new validation.

## 2026-07-21T21:11:54+02:00 - Let the clean redirect-error validation finish

**Context:** Search Console retains three "Redirect error" examples last crawled on 11 April 2026: the retired `/blog/eu-pet-passport-germany-2025` URL and the canonical `/vets/frankfurt` and `/vets/hamburg` pages. Validation started on 21 July and currently shows all three pending with zero failures. The examples predate the 21 June Nginx canonical-path correction that stopped no-trailing-slash pages from being redirected through their prerendered directories.

**Decision:** Make no additional code or Search Console changes while the active validation runs. The correct current contract is one direct permanent redirect from the retired passport URL and no redirect for either canonical city URL.

**Evidence:** Live Googlebot-style requests show the retired passport URL reaches its evergreen replacement in exactly one redirect and finishes with 200. Frankfurt and Hamburg both return 200 with zero redirects. The sitemap contains only the evergreen passport URL plus both canonical city URLs. Current internal links use the evergreen passport path, and exact Nginx rules cover both trailing variants of its retired path.

**Trade-offs:** Search Console may continue showing the stale error until Google recrawls all three URLs. Forcing another validation or changing already-correct routes would not accelerate that crawl and could reintroduce the canonicalization regression.

**Follow-up:** Allow the current validation to finish. Reopen this issue only if Search Console records a new failed sample or a live smoke test finds a loop, chain longer than one hop, broken destination, or redirect on a canonical city URL.

**Rollback:** This decision adds documentation only and changes no runtime behavior. If a live regression appears, use the SEO smoke test and Nginx access logs to isolate the exact request before modifying the canonical-path rule.

## 2026-07-21T21:22:55+02:00 - Require route-specific metadata before prerender capture

**Context:** Search Console lists 84 "Alternative page with proper canonical tag" examples: 77 historical contact query URLs canonicalized to `/contact` and seven trailing-slash location aliases. Live redirect checks show all seven aliases now reach their no-slash canonical in one hop, while contact queries remain functional and correctly canonicalize to `/contact`. During raw HTML verification, canonical location and blog URLs such as `/vets/berlin/tempelhof` and `/blog/eu-pet-passport-germany` were found serving the homepage title and canonical before JavaScript rendering. The same defect was reproduced in the existing local `dist` output.

**Root cause:** The prerender readiness check accepted any Helmet-managed canonical. After the homepage was rendered into the fallback `index.html`, later lazy routes immediately satisfied that check with the stale homepage canonical and could be captured before their route component updated the head.

**Decision:** Calculate the expected canonical for every prerender route and wait until Helmet emits that exact URL. The noindexed `/404` route, which intentionally has no canonical, waits for its route-specific title instead. Fail the prerender command when any route fails or the rendered count is incomplete. Support an explicit `PRERENDER_DIST_DIR` so CI and audit verification can generate into an isolated directory without overwriting an existing build.

**Trade-offs:** Prerendering waits for the correct lazy route instead of using a short generic readiness signal, so builds take longer but become deterministic. A route with missing or incorrect canonical metadata now fails the build rather than silently publishing homepage metadata or missing output.

**Verification:** TDD RED first failed because the route-readiness module did not exist, then failed because the isolated output resolver was absent. GREEN covers root, location, blog, and no-canonical 404 expectations plus incomplete-run rejection and isolated output selection. An isolated production build prerendered all 230 routes successfully. A post-render audit found zero canonical mismatches and zero non-homepage fallback titles. The full gates pass: 25 API tests, 63 frontend tests across 14 files, ESLint, TypeScript, Node syntax, shell syntax, and `git diff --check`. The 12 MB temporary build was moved to macOS Trash after verification and can be recovered there until Trash is emptied.

**Search Console handling:** Leave the active validation unchanged while Google recrawls. The 77 contact alternatives are legitimate legacy URLs and no longer originate from crawler-facing report links. The seven trailing aliases are now redirects and should leave the alternative category after recrawl. Do not redirect all contact queries because that would break supported legacy prefilled forms.

**Production rollout:** Deployment remains out of scope. When the normal deployment process eventually runs, the postbuild prerender must complete all routes and the generated HTML audit should confirm representative city, district, blog, and contact canonicals before release.

**Rollback:** Revert `scripts/prerender-readiness.js`, its test, and the exact-canonical integration in `scripts/prerender.js`. This restores timing-based prerendering and is not recommended because it can silently publish homepage metadata on non-homepage routes.

## 2026-07-21T21:35:24+02:00 - Measure prerender metadata before rewriting strong landing pages

**Context:** The Search Console 28-day baseline is 28 clicks, 2,741 impressions, 1.0% CTR, and average position 15.3. Mobile produced 19 clicks from 972 impressions at 2.0% CTR; desktop produced 9 clicks from 1,761 impressions at 0.5% CTR. Germany is the main market with 1,183 impressions and 13 clicks, while the United States contributes 584 impressions and 4 clicks, consistent with people researching a move before arriving.

**Evidence:** The strongest zero-click query is `24hr vet near me` with 87 German impressions at position 6.5, all landing on the Hamburg emergency guide. The guide already uses a 24-hour Hamburg emergency title, description, H1, and detailed clinic content, but its live raw HTML serves the homepage title and canonical. Pet-friendly Berlin apartment variants contribute about 80 recent impressions at positions 17 to 23, and the target article already names Berlin in its title, description, H1, and FAQ while its live raw head also contains homepage metadata. Other page-one pages with low CTR, including tick prevention, breed restrictions, and cat microchipping, share the same prerender failure pattern.

**Decision:** Do not rewrite these already aligned titles or content until the route-specific prerender correction is released and recrawled. Treat correct raw metadata as the primary CTR experiment. Preserve the current content so the effect can be measured without a simultaneous copy change.

**Trade-offs:** Waiting delays speculative snippet experiments, but avoids weakening pages whose visible content already matches demand. It also prevents attributing any CTR movement to the wrong change. The homepage remains a separate future optimization candidate at 444 impressions, 1.6% CTR, and position 12.3.

**Follow-up:** After the normal release and Google recrawl, compare a fresh 28-day period against this baseline. Prioritize title or description experiments only for pages that still have at least 50 impressions, average position 12 or better, and below 1% CTR with verified route-specific raw metadata.

**Rollback:** This entry records a measurement strategy and changes no runtime behavior. If query intent or the page content changes materially, establish a new baseline before testing copy.

## 2026-07-21T22:00:10+02:00 - Remove the mobile homepage's avoidable performance costs

**Context:** Search Console has insufficient Chrome UX Report field data for both mobile and desktop, so the audit used a PageSpeed Insights mobile lab run as its baseline. The live homepage scored 59 for Performance with FCP at 4.6 seconds, LCP at 8.2 seconds, TBT at 200 milliseconds, and CLS at 0. The largest concrete findings were a 343 KiB square JPEG served as `/logo.png` at roughly 112 pixels, a Google Fonts stylesheet discovered serially through a CSS `@import`, and 270 KiB of estimated unused JavaScript. The unused-JavaScript detail attributed about 185 KiB to Google Maps, 65 KiB to Analytics, and 20 KiB to first-party code. The map was hidden on mobile with CSS but its provider still loaded the full Maps SDK.

**Decision:** Keep the original logo for Open Graph and structured-data references, but use a 256 by 256 WebP asset with explicit dimensions for visible logo placements. Move the Google Fonts stylesheet from the CSS `@import` to the document head with preconnect hints so discovery occurs in parallel. On mobile, do not load the Maps provider, its local chunks, or the external SDK during initial page load. Replace location autocomplete with an accessible activation control that loads the feature on demand. Continue loading the map immediately on desktop where it is visible. Remove interactive Maps preload hints from prerendered HTML. Leave Analytics behavior unchanged because delaying it would alter consent-mode measurement and requires a separate product decision.

**Trade-offs:** The first mobile tap on location search now initiates the Maps download before the autocomplete becomes available. This adds a small interaction delay for visitors who use that optional feature, while removing the much larger third-party cost for every mobile visitor who does not. Desktop behavior and all city filters remain unchanged. Retaining Google-hosted fonts preserves the current visual design but does not achieve the maximum savings possible from system fonts or self-hosting.

**Verification:** TDD RED confirmed the missing optimized logo component, serial font import, unconditional mobile Maps mounting, and unwanted interactive preload hints. GREEN covers mobile and desktop activation behavior, visible-logo attributes, font discovery, and prerender filtering. An isolated production build completed and all 230 routes prerendered successfully. A 390 by 844 browser run recorded zero Maps-related local or external requests before activation; after activation it loaded the provider, vendor, autocomplete, and external API as intended, without loading the hidden desktop map component. The generated homepage contains the 2,194-byte 256 by 256 WebP, the font preconnects, and no interactive Maps preload. Full gates pass with 25 API tests and 71 frontend tests, plus ESLint, TypeScript, Node syntax, and production build checks.

**Production rollout:** No deployment was performed. After the normal release, rerun PageSpeed Insights on the live homepage and compare mobile FCP, LCP, transferred image bytes, and unused JavaScript against this baseline. Field Core Web Vitals will remain unavailable until Google has enough real-user samples.

**Rollback:** Restore the direct `/logo.png` image tags, remove `BrandLogo` and `logo-256.webp`, restore the Google Fonts CSS `@import`, and remove the head font links. Restore the unconditional `APIProvider` wrapper in `Home.tsx`, the direct `PlaceAutocomplete` render in `VetFilters.tsx`, and the previous prerender preload filter. This reintroduces the measured mobile costs but restores the exact prior loading sequence.

## 2026-07-21T22:11:43+02:00 - Correct the homepage accessibility audit findings

**Context:** The same mobile PageSpeed run scored the live homepage at 93 for Accessibility. Its detailed audit identified three problems: no main landmark, repeated `Visit Website` link names pointing to different practices, and insufficient foreground/background contrast. The contrast failures covered the orange brand wordmark, small filter labels and inactive city buttons, result and pagination metadata, district badges, card addresses and English-signal text, mobile navigation labels, and the cookie consent accept button.

**Decision:** Mark the directory column as the page's single `<main>` landmark. Give every practice website link an accessible name containing the practice name while keeping the visible `Visit Website` label. Preserve the established palette but use the existing dark `accent-ink` token whenever orange is rendered as text or as a button background, increase small green text from 40 or 60 percent opacity to accessible 70 or 80 percent variants, use gray 600 for verification dates, and render the mobile navigation labels at full cream opacity.

**Trade-offs:** Some orange text and buttons become a darker rust color, and secondary labels become slightly more prominent. This is a deliberate visual change required for readable contrast. The visible wording of repeated practice links remains compact, while screen readers receive a longer practice-specific purpose.

**Verification:** TDD RED reproduced all three audit failures through missing landmark, ambiguous link-name, and low-contrast class assertions. GREEN passes the targeted component tests. Calculated contrast ratios for the replacement combinations range from 4.61:1 to 7.56:1. An isolated production build rendered a browser accessibility tree with one main landmark and unique website-link names for every visible practice. All 230 routes prerendered successfully. Full gates pass with 25 API tests and 77 frontend tests across 21 files, plus ESLint, TypeScript, Node syntax, production build, and `git diff --check` validation.

**Production rollout:** No deployment was performed. After the normal release, rerun PageSpeed Insights against the live homepage to confirm the Accessibility score and verify that no new contrast failures are reported from the updated mobile location-search activation state.

**Rollback:** Change the homepage `<main>` back to its former `<div>`, remove the practice-specific website `aria-label`, restore the prior opacity and accent utility classes in the homepage, filters, cards, pagination, mobile navigation, and cookie consent components, and remove the new accessibility tests. This restores the exact previous presentation but also restores the verified audit failures.

## 2026-07-21T22:58:27+02:00 - Make directory interactions keyboard and dialog accessible

**Context:** The interaction audit found that the report overlay had no dialog semantics, initial focus, focus trap, Escape handling, background isolation, or focus restoration. The More Cities popup did not expose its state or option relationship and had no arrow-key navigation. Practice cards selected the directory map only through a pointer click, icon-only map and report controls were ambiguous or undersized, mobile navigation targets were undersized, and the application had no consistent high-visibility keyboard focus treatment.

**Decision:** Extract the report flow into a portaled modal dialog with a practice-specific accessible name, initial Close focus, Tab containment, Escape dismissal, background `inert` and `aria-hidden` isolation, scroll locking, and return focus. Treat More Cities as a listbox popup with expanded state, option roles, arrow, Home, End, Tab, and Escape behavior. Add a named explicit directory-map selection button on desktop while retaining card pointer selection as an enhancement. Give external map and report controls practice-specific names and 44 pixel minimum targets, extend that target size to mobile navigation, and apply a dual-ring `:focus-visible` indicator across native and custom interactive elements.

**Trade-offs:** The directory card gains one explicit desktop map action, and keyboard focus is visually stronger than the previous design. The modal manages body and application-root state while mounted, so its cleanup restores every prior value rather than assuming defaults. A native `dialog` element was considered, but the portaled ARIA dialog preserved the existing visual structure and routing links with less unrelated change.

**Verification:** TDD RED reproduced the missing dialog contract, focus containment and restoration, popup keyboard behavior, accessible control names, minimum targets, and focus styling. GREEN passes 82 frontend tests across 24 files and 25 API tests. TypeScript, ESLint, Node syntax, and `git diff --check` are clean. Browser inspection confirmed one named modal dialog, initial Close focus, `aria-modal`, locked scrolling, an inert and hidden application root, a named listbox with 29 options, and restored semantic control names. An isolated production build completed and all 230 routes prerendered successfully with 230 route index files.

**Production rollout:** No deployment was performed. After the normal release, repeat keyboard-only checks at 390 pixel and desktop widths, then rerun the live accessibility audit to confirm focus order, target sizing, and dialog behavior in the deployed bundle.

**Rollback:** Restore the inline report overlay in `Home.tsx`, remove `ReportIssueDialog` and its tests, restore the prior More Cities click-only implementation, remove the explicit directory-map button and practice-specific icon labels, revert the mobile target classes and global focus rule, and restore the former `VetCard` callback signature. This restores the previous presentation but also restores the verified keyboard and modal barriers.

## 2026-07-21T23:07:27+02:00 - Announce and focus public form failure states

**Context:** Browser and source audits found that failed Contact submissions disabled the focused Submit button, moved focus to the document body, and rendered an unannounced error. Successful Contact submissions replaced the form without a status announcement or focus target. Practice confirmations repeated the same focus loss and exposed neither alert nor status semantics, while their repeated button names omitted the practice name. Location search had no programmatic input label, used blocking browser alerts for geolocation failures, and provided undersized icon controls. The cookie notice was visible but had no named landmark. Native Contact field validation already focused the first invalid control correctly and did not need replacement.

**Decision:** Preserve native field validation and add focused result containers for Contact API errors and successful completion. Keep async submit controls in the focus order with `aria-disabled` and guard duplicate requests in their handlers. Give every confirmation action a practice-specific name, keep focus stable while it submits or fails, announce failure as an alert, and focus a polite status after success. Label both location-search states, replace blocking alerts with inline alert text, and give Clear and GPS controls explicit names and 44 pixel targets. Expose the cookie notice as a region named by its heading.

**Trade-offs:** Programmatically focusing result messages adds a deliberate focus move after the server resolves, which is appropriate because the result is the next required information. `aria-disabled` controls remain technically clickable, so event handlers must enforce the disabled state; both Contact and confirmation handlers now do so. Inline location errors take a small amount of vertical space but preserve context and remain available for review, unlike transient blocking alerts.

**Verification:** TDD RED produced seven failures for the missing result roles, focus behavior, practice-specific names, location label and error, target sizes, and cookie region. GREEN passes 86 frontend tests across 26 files and 25 API tests. TypeScript and ESLint pass. Browser interaction confirmed that a Contact network failure focuses its alert and that a confirmation failure remains focused on a uniquely named retry control while exposing an alert. An isolated production build completed and all 230 routes prerendered successfully with 230 route index files.

**Production rollout:** No deployment was performed. After the normal release, verify one failed and one successful Contact request with a screen reader, reject geolocation permission once to confirm the inline error, and confirm that repeated practice actions are announced with distinct names.

**Rollback:** Remove the Contact result ref, roles, focus effect, busy state, and `aria-disabled` guard; restore the disabled confirmation and Contact controls; remove confirmation result semantics and practice-specific names; restore location `alert()` calls and the previous icon sizes; and remove the cookie region attributes and new tests. This restores the prior interactions and their verified focus and announcement failures.

## 2026-07-21T23:17:32+02:00 - Reject unreachable production components

**Context:** A placeholder-behavior audit found `EmailCourseSignup.tsx`, which simulated a successful subscription after a timeout without contacting any service. Dependency tracing showed that it had no import, route, or render site, so no visitor could reach it. The same trace found `VetSchema.tsx`, an unused structured-data component that duplicated the active schema utilities and expected an obsolete vet data shape. Keeping either file created a risk that future work could accidentally expose deceptive behavior or reuse incompatible schema code.

**Decision:** Delete both unreachable components rather than integrate or repair features that the product does not expose. Add a production reachability test that starts at `src/main.tsx`, follows static and dynamic local imports, and fails when any non-test component under `src/components` is outside the application graph.

**Trade-offs:** The reachability guard assumes production components belong in the entry-point graph. Intentionally dormant components will now require an explicit architectural decision instead of silently remaining in the source tree. A real email course can still be built later, but it must start with a provider, consent requirements, failure handling, and a user-visible product decision rather than the deleted simulation.

**Verification:** TDD RED identified exactly `components/EmailCourseSignup.tsx` and `components/VetSchema.tsx`. GREEN passes after deleting those two files. The full gates pass with 87 frontend tests across 27 files, 25 API tests, TypeScript, ESLint, and `git diff --check`. Searches find no remaining fake-success markers or references to either component. An isolated production build completed and all 230 routes prerendered successfully with 230 route index files.

**Production rollout:** No deployment was performed. The deleted components were unreachable, so release behavior should remain unchanged. If an email signup becomes a real product requirement, define its provider, lawful consent copy, double-opt-in behavior, API contract, analytics event, and failure states before implementation.

**Rollback:** Restore `web-app/src/components/EmailCourseSignup.tsx` and `web-app/src/components/VetSchema.tsx` from version control and remove `sourceReachability.test.ts`. The files are recoverable from repository history, but restoring the fake signup without a real service is not recommended.

## 2026-07-21T23:26:21+02:00 - Extend reachability checks to modules and assets

**Context:** After the component cleanup, the same application-graph audit was expanded to all production TypeScript modules and source assets. It found one unreachable utility, `utils/geojson.ts`, which had not been imported since its initial map implementation and duplicated the map's current direct marker conversion. Asset tracing found the original React and Vite scaffold SVGs. `public/vite.svg` was copied into every build despite having no reference. Seven unreferenced JSON files under `src/data` were reviewed separately and contain historical verification, removal, and URL-audit evidence rather than application code.

**Decision:** Delete the unreachable GeoJSON utility and both default scaffold SVGs. Expand the reachability test to reject any non-test TypeScript module or source asset that cannot be reached from `src/main.tsx`, including dynamically imported routes. Add a specific guard against shipping the default public Vite asset. Preserve the historical vet datasets because they provide operational provenance and are not included in the production bundle.

**Trade-offs:** The application graph is now intentionally closed, so future standalone TypeScript modules must either become a documented operational entry point outside `src` or be integrated before they are retained. Historical audit JSON remains in `src/data`, which is not ideal organization, but moving or deleting it without a retention policy would risk losing useful sourcing evidence. Production-output verification confirms those files are not exposed.

**Verification:** The expanded module test failed first on `utils/geojson.ts`; after deletion it passed. The new asset tests failed first on `assets/react.svg` and `public/vite.svg`; after deletion all three reachability checks passed. Full gates pass with 89 frontend tests across 27 files, 25 API tests, TypeScript, ESLint, and `git diff --check`. An isolated build contains neither scaffold SVG nor any preserved audit dataset. All 230 routes prerendered successfully with 230 route index files.

**Production rollout:** No deployment was performed. The deleted files were unreachable or unreferenced, so runtime behavior is unchanged. The normal release should simply omit the previously copied `vite.svg` file.

**Rollback:** Restore `web-app/src/utils/geojson.ts`, `web-app/src/assets/react.svg`, and `web-app/public/vite.svg` from version control, then narrow or remove the corresponding reachability assertions. All three files remain recoverable from repository history.

## 2026-07-21T23:33:19+02:00 - Retire unsafe legacy web-app maintenance scripts

**Context:** The maintenance-script security audit mapped every executable under `web-app/scripts`. Eight files were obsolete one-off utilities outside the package scripts and build graph. Three depended on an old `C:\Users\...` workspace and missing CSVs. Four could directly overwrite `src/data/vets.json` or the retired internal CSV without a dry-run, backup, atomic replacement, schema validation, or rollback. `maintenance.js` was especially misleading: it used random numbers and mock OSM output, then reported 99 percent database integrity as though it had performed a real check. The web-app README instructed operators to run that fake command monthly.

**Decision:** Delete the eight superseded scripts: `audit_frankfurt.py`, `audit_urls.py`, `cleanup-vets.js`, `cleanup_vets_v2.py`, `fix_db.py`, `geocode.js`, `maintenance.js`, and `sync_vets_data.py`. Replace the README's fake maintenance instructions and outdated architecture claims with a clear operational warning that dataset changes require a reviewed, recoverable workflow. Add a script-safety test that rejects the retired filenames, developer-specific Windows user paths, and simulated maintenance claims within executable web-app scripts.

**Threat model:** The primary actor is a well-intentioned operator following stale documentation. The attack-equivalent path is accidental tampering: run an apparently routine command, silently rewrite the canonical dataset using stale heuristics or missing inputs, and accept fabricated health output. Impact is high because every public listing and generated route derives from `vets.json`; likelihood was medium because the README explicitly recommended the command. Removing the execution path is safer than retrofitting ambiguous historical migration logic.

**Trade-offs:** The deleted scripts can no longer be used as informal examples, but version control preserves them if historical logic must be inspected. Repository-root maintenance scripts remain a separate audit scope and are not implicitly approved by this decision. Real link-health monitoring is still absent; the change removes a false claim rather than pretending to replace it.

**Verification:** TDD RED first identified all eight retired files plus four path or simulation violations. After excluding the test fixture itself, the same intended failures remained. GREEN passes after deletion and documentation correction. Full gates pass with 91 frontend tests across 28 files, 25 API tests, TypeScript, ESLint, JavaScript syntax checks, shell syntax validation, and `git diff --check`. An isolated production build completed and all 230 routes prerendered successfully with 230 route index files.

**Production rollout:** No deployment was performed. The scripts were outside the runtime and build graph, so public behavior is unchanged. Do not substitute an unreviewed repository-root script for any removed command.

**Rollback:** Restore the eight files and the former web-app README from version control, then remove `scripts/script-safety.test.js`. All deleted files are recoverable from repository history. Restoring `maintenance.js` or its README instruction is not recommended because its results were fabricated.

## 2026-07-21T23:42:56+02:00 - Restrict repository maintenance to the reviewed confirmation sweep

**Context:** The repository-root maintenance audit found 18 historical one-off Python scripts with no current CI, application, package, or operational call sites. These scripts could overwrite the canonical vet JSON, internal CSV data, generated files, or a local SQLite table without a required dry run, backup, atomic replacement, schema validation, or rollback. Several embedded developer-specific Windows paths, accepted a Google API key through a process-visible command argument, or issued external geocoding requests without a timeout. The root README still directed operators to run `cleanup_vets.py` against the canonical dataset.

**Decision:** Retire the 18 unreferenced mutators and retain only `sweep_confirmations.py`, which has a dry-run mode, explicit subprocess timeouts, `shell=False`, unknown-ID safeguards, and behavioral tests. Replace the unsafe README cleanup command with a dry-run-first confirmation workflow and a maintenance-test command. Add a repository guard that permits only reviewed production maintenance scripts and rejects developer-specific paths in supported workflows.

**Threat model:** The main threat is accidental or compromised-operator tampering with the canonical directory dataset. Missing backups and audit records weaken repudiation controls, command-line API keys risk information disclosure, and unbounded external requests create denial-of-service and cost exposure. Because the obsolete utilities were undocumented migrations with no active callers, removing their executable surface is lower risk than retrofitting undefined historical behavior.

**Trade-offs:** Historical repair logic is no longer directly executable from the working tree, but every retired script remains recoverable from Git. The supported confirmation sweep still deserves a separate atomic-write and malformed-log-record review; retaining it here does not approve every implementation detail. Ingestion workflows are outside this focused maintenance cleanup.

**Verification:** TDD RED first failed with all 18 unsupported production scripts present. GREEN passes all 6 maintenance tests after retirement and documentation correction. Repository-wide reference search finds no hidden caller for the deleted files. Full gates pass with 91 frontend tests across 28 files, 25 API tests, TypeScript, ESLint, Python and JavaScript syntax checks, and `git diff --check`. An isolated production build completed and all 230 routes prerendered successfully. Its temporary build was moved to Trash and the preview port closed.

**Production rollout:** No deployment was performed. The retired scripts are outside the runtime and build graph, so public behavior is unchanged.

**Rollback:** Restore the deleted `scripts/maintenance` files and the former root README from version control, then remove `test_script_safety.py`. Restoring the unsafe cleanup instruction or running a retired mutator against canonical data is not recommended.

## 2026-07-21T23:59:22+02:00 - Make the confirmation sweep fail closed and recoverable

**Context:** The retained confirmation sweep crosses two trust boundaries: an append-only JSONL log fetched over SSH and the canonical `vets.json` dataset used to generate the public directory. It parsed remote records permissively, read the entire remote log into memory, relied on an `assert` for CRLF validation, overwrote the canonical file directly, deleted malformed records during a successful trim, and reported a remote trim failure as a successful process exit. An interrupted local write could corrupt the dataset, while a far-future date could permanently poison a practice's `last_scanned` value.

**Decision:** Validate the entire remote batch before reading local data and reject malformed JSON, non-object records, missing or oversized vet IDs, invalid or future dates, oversized lines, and batches beyond 1,000 records. Limit the SSH read to 1,001 lines so overflow can be detected without loading the full log. Replace assertion-based line-ending checks with explicit runtime validation. Stage the original and updated datasets in the target directory, flush them to disk, atomically rotate the original to `vets.json.bak`, atomically replace the canonical file, preserve its permissions, and ignore the generated backup in Git. Treat remote trim failure as an incomplete sweep and avoid rewriting an idempotent replay so the pre-change backup remains useful.

**Threat model:** A compromised remote log or careless operator can attempt tampering through malformed structures, future timestamps, or resource-exhausting payloads. Direct writes create denial-of-service and integrity risks if interrupted. Silent trim failure weakens repudiation because the operator cannot distinguish a completed acknowledgment from a partial transaction. The implemented controls bound input, fail before mutation, keep unacknowledged records remote, preserve local rollback data, and return a failing status for incomplete acknowledgment.

**Trade-offs:** One malformed or unknown record can intentionally block remote-log trimming until a human reviews it. This favors data preservation over unattended progress. The backup is rotating rather than historical, so Git remains the durable history after a successful reviewed change. The local commit and remote trim are not a distributed transaction; idempotent replay and nonzero trim failure are the focused mitigation without redesigning the VPS queue.

**Verification:** TDD RED reproduced 17 failures across untrusted-record parsing, batch and line bounds, explicit CRLF validation, atomic replacement, backup and permission preservation, temporary-file cleanup, idempotent replay, backup tracking, and remote trim failure. GREEN passes all 21 maintenance tests. Full gates pass with 91 frontend tests across 28 files, 25 API tests, TypeScript, ESLint, Python and JavaScript syntax checks, shell syntax validation, and `git diff --check`. An isolated production build completed and all 230 routes prerendered successfully. Its temporary output and generated test caches were moved to Trash, and the preview port closed.

**Production rollout:** No deployment or live SSH execution was performed. Before the next real sweep, run the maintenance suite and `--dry-run`, inspect any invalid or unknown line numbers, and preserve the reported backup until the resulting dataset diff is accepted.

**Rollback:** Restore `sweep_confirmations.py`, `test_sweep_confirmations.py`, `test_script_safety.py`, `.gitignore`, and the README from version control. If a real sweep must be rolled back before committing, replace `web-app/src/data/vets.json` with the adjacent `vets.json.bak` and rerun the maintenance tests before any remote log is trimmed.

## 2026-07-22T00:08:19+02:00 - Validate confirmation IDs before producer side effects

**Context:** The public `/api/confirm-vet` endpoint required only a practice name. A missing or non-string `vetId` was sanitized to an empty string, an oversized ID was silently truncated, and malformed identifiers could be transformed into a different value before being appended to the confirmation JSONL log. The hardened maintenance sweep correctly rejects those records, but one invalid public submission could therefore block the entire batch until manual repair.

**Decision:** Require `vetId` to be a nonempty string, trim only surrounding whitespace, reject values beyond 100 characters instead of truncating them, and permit only ASCII letters, digits, dots, underscores, and hyphens. Apply this validation before JSONL logging or SMTP notification. The current dataset contains 250 nonempty IDs, all at most 34 characters and compatible with the selected syntax, and the frontend posts the selected practice's stored ID.

**Threat model:** The primary actor is an unauthenticated malicious client or malformed frontend request. The attack path submits an empty, transformed, oversized, or control-character ID, causing queue tampering and maintenance denial of service. Strict raw-value validation closes that path and the existing endpoint and Nginx rate limits bound repeated attempts. A syntactically valid but nonexistent ID remains a medium spoofing risk because the API does not have a synchronized server-side allowlist; the sweep preserves such records for review rather than applying them.

**Trade-offs:** The API remains independent of the frontend dataset, avoiding duplicated allowlists and deployment synchronization failures. That independence means syntax validation cannot prove practice existence. Loading or generating an allowlist, signing practice identifiers, or adding a quarantine acknowledgment flow requires a separate architectural decision.

**Verification:** TDD RED reproduced 10 failures covering missing, non-string, oversized, whitespace, HTML-like, path-like, and control-character IDs plus endpoint side effects. GREEN passes all 15 focused confirmation tests and the complete suite of 35 API tests, 21 maintenance tests, and 91 frontend tests across 28 files. TypeScript, ESLint, Python and JavaScript syntax checks, shell syntax validation, and `git diff --check` pass. An isolated production build completed and all 230 routes prerendered successfully. Its temporary output and generated test caches were moved to Trash, and the preview port closed.

**Production rollout:** No deployment or live API request was performed. After the normal release, verify one valid confirmation and confirm that a missing or malformed `vetId` returns HTTP 400 without a JSONL append or email.

**Rollback:** Remove `MAX_VET_ID_LEN`, `VET_ID_REGEX`, and the raw `vetId` validation block from `api/main.py`, restore the former sanitized and truncated return value, and remove the new confirmation validation tests. This restores compatibility with malformed requests but also restores the verified queue-blocking path.

## 2026-07-22T00:18:37+02:00 - Restrict confirmations to an active practice ID manifest

**Context:** Syntax validation prevented malformed confirmation IDs but still accepted any well-formed value. An anonymous caller could submit an invented value such as `Ghost-99`, causing the maintenance sweep to preserve the unknown record and leave the remote batch untrimmed. The API and canonical frontend dataset are deployed from the same pulled repository, so a committed API-local manifest can provide existence validation without request-time access to frontend files.

**Decision:** Generate `api/valid_vet_ids.json` from canonical entries whose verification status is not `Permanently Closed`. Load it once at API startup as an immutable set and reject confirmation IDs not present in it before logging or email. Validate that the manifest is a nonempty, sorted, unique JSON array whose entries satisfy the established ID length and syntax contract. Fail API startup if the manifest is missing, unreadable, malformed, duplicated, unsorted, or contains an invalid entry. Add a synchronization test that compares all 248 manifest IDs with the canonical active dataset and therefore fails whenever dataset maintenance changes the allowlist without updating the API artifact.

**Threat model:** The primary actor is an unauthenticated bot submitting syntactically valid invented or closed practice IDs. The attack targets availability by creating an unprocessable queue head and targets integrity by associating evidence with a nonexistent record. Constant-time membership validation removes that path. Startup validation treats the committed manifest as a trust boundary and fails closed rather than silently bypassing authorization if its integrity is lost.

**Trade-offs:** The manifest duplicates active IDs and must change with the canonical dataset, but the synchronization test makes drift visible before release and keeps request handling independent of filesystem layout. Permanently closed practices are intentionally excluded. The allowlist proves that a practice exists; it does not prove that an anonymous confirmer visited the practice or reported truthfully. Rate limiting and human-review semantics remain necessary controls for that product-level risk.

**Verification:** TDD RED reproduced 9 failures covering invented and closed IDs, endpoint side effects, missing manifest data, malformed JSON, wrong structure, duplicates, invalid entries, and missing loader behavior. GREEN passes all 19 focused ID tests and the full suite of 44 API tests, 21 maintenance tests, and 91 frontend tests across 28 files. TypeScript, ESLint, Python and JavaScript syntax checks, shell syntax validation, and `git diff --check` pass. An isolated production build completed and all 230 routes prerendered successfully. Its temporary output and generated test caches were moved to Trash, and the preview port closed.

**Production rollout:** No deployment or live endpoint request was performed. Before any later release that changes `vets.json`, run the API suite and update `valid_vet_ids.json` until the synchronization test passes. After release, verify a current practice ID succeeds and an invented or permanently closed ID returns HTTP 400 without a JSONL append or email.

**Rollback:** Remove `VALID_VET_IDS_PATH`, `load_valid_vet_ids`, `VALID_VET_IDS`, and the membership check from `api/main.py`; delete `api/valid_vet_ids.json`; and remove the manifest and unknown-ID tests. This restores acceptance of well-formed invented IDs and the verified queue-blocking risk.

## 2026-07-22T00:28:35+02:00 - Preserve anonymous confirmations as pending evidence only

**Context:** The confirmation endpoint is intentionally anonymous. Even after strict active-ID validation, any caller could submit a real practice ID. The maintenance sweep treated one such submission as authoritative by adding it to public `verification.english_signals`, advancing `verification.last_scanned`, and setting `community_status` to `Verified`. Those fields drive directory filters, visible badges, listing evidence, city summaries, district SEO copy, and factual claims about expat owners being seen at a practice.

**Decision:** Transfer anonymous submissions into a separate `pending_community_confirmations` array containing only the submission date and fixed `site_button` source. Deduplicate by practice, date, and source. Preserve `community_status`, `verification.status`, `verification.last_scanned`, and `verification.english_signals` exactly. Validate existing pending state before mutation and stop before local writes or remote trimming if it is malformed. Update the API notification and public success message to request human review rather than promising an automatic refresh or recommending verification promotion.

**Threat model:** A malicious or mistaken anonymous visitor can legitimately name an active practice but cannot prove a visit or truthful English-language experience. Automatically converting that assertion into verification evidence enables spoofing and data tampering that propagates into public content and search claims. Separating pending evidence removes the privilege escalation from anonymous input to trusted publication while preserving a reviewable, idempotent record.

**Trade-offs:** Confirmations no longer improve public verification automatically, so a human must review pending entries and deliberately promote supported evidence. The pending date and source are included in the frontend dataset bundle even though no public component displays them; they contain no personal data. A separate private review store would reduce public metadata exposure but would require a multi-file transaction and operational review system outside this focused change.

**Verification:** TDD RED reproduced failures for automatic status promotion, `last_scanned` mutation, trusted-signal insertion, duplicate handling, malformed pending state, unsafe remote acknowledgment, promotional email wording, and misleading success copy. GREEN passes all 25 maintenance tests, 45 API tests, and 91 frontend tests across 28 files. TypeScript, ESLint, Python and JavaScript syntax checks, shell syntax validation, and `git diff --check` pass. An isolated production build completed and all 230 routes prerendered successfully. Its temporary output and generated test caches were moved to Trash, and the preview port closed.

**Production rollout:** No deployment, live SSH sweep, or live endpoint request was performed. After the normal release, submit one valid test confirmation, verify it enters pending review without changing public verification fields, then remove or adjudicate the test record before publication.

**Rollback:** Restore the former `apply_confirmations` promotion logic and API and component messages, remove `pending_community_confirmations` from the `Vet` type, and restore the prior tests. This also restores the verified path from anonymous input to authoritative public evidence and is not recommended.

## 2026-07-22T00:48:56+02:00 - Prioritize existing search demand and remove fallback metadata conflicts

**Context:** The linked Search Console report in GA4 showed that the current and legacy pet-friendly housing URLs generated 681 impressions but only 2 clicks over the last 28 days. The Hamburg emergency-vet URL variants generated 226 impressions and 2 clicks. Named apartment queries generated another 93 impressions with no clicks, including 71 impressions for Berlin-specific pet-friendly apartment variants. The original titles placed the strongest query terms late or exceeded practical snippet length. Isolated prerender verification then found a second root cause: every route retained the generic homepage description and Open Graph metadata from `index.html` alongside its Helmet metadata, leaving crawlers with conflicting route descriptions.

**Decision:** Put the observed query language first in the housing and Hamburg emergency title, description, and H1. Keep titles at 52 and 59 characters, respectively. Add contextual links from the housing city's district cards to the Berlin, Hamburg, and Frankfurt vet directories. Change `generateArticleSchema` so `dateModified` defaults to the stable publication date and accepts a separately verified modification date; do not derive freshness from the build clock. Record 22 July 2026 as the explicit modification date for the two substantively edited pages. Mark generic route-sensitive metadata in `index.html` as prerender fallback data and remove it only after Helmet has emitted route-specific metadata.

**Trade-offs:** Google can rewrite titles and descriptions, so improved CTR is a measured hypothesis rather than a guarantee. Year-bearing titles require a real annual review instead of an automatic text change. Pages that do not emit a route-specific social tag no longer inherit misleading homepage social metadata in prerendered HTML. Stable schema dates may look older than build-generated dates, but they accurately represent verified content changes and avoid false freshness signals.

**Verification:** TDD RED reproduced build-clock schema dates, the two weaker snippets, missing contextual city links, and duplicate prerender metadata. GREEN passes 96 frontend tests across 30 files, ESLint, TypeScript, JavaScript syntax checks, and whitespace validation with CRLF-aware Git settings. An isolated production build completed and all 230 routes prerendered. A complete scan of the 230 HTML files found no retained fallback metadata and no duplicate description or Open Graph title tags. Both target pages contain their expected titles and explicit `dateModified` value. Temporary build output was moved to Trash and preview port 4174 closed.

**Production rollout:** No deployment or external Google setting change was performed. After the normal release and recrawl, compare a complete 28-day window against the 681-impression housing and 226-impression Hamburg baselines. Evaluate clicks and CTR by canonical landing page, while accounting for legacy URL signal consolidation and ranking changes.

**Rollback:** Restore the previous target-page metadata and H1 text, remove the three contextual directory links, restore build-clock `dateModified`, remove the prerender fallback markers and cleanup step, and remove the associated tests. Restoring duplicate fallback metadata or fabricated build-date freshness is not recommended.

## 2026-07-22T01:04:16+02:00 - Improve two page-one zero-click article snippets

**Context:** The linked Search Console data showed `/blog/breed-restrictions-germany` with 67 impressions, no clicks, and an average position of 9.99 over the last 28 days. `/blog/cat-microchipping-germany` had 23 impressions, no clicks, and an average position of 6.87. Both pages therefore already had page-one visibility but their titles and descriptions did not directly answer the observed search intent. Source verification also found that the cat article's legal summary was outdated: Berlin has a citywide cat-protection ordinance and Hamburg's citywide ordinance took effect on 1 January 2026, while the federal framework permits geographically defined local rules rather than imposing one nationwide microchipping duty.

**Decision:** Lead each title, description, and H1 with the specific search question and keep the year tied to a verified July 2026 review. Set `dateModified` explicitly to `2026-07-22` while preserving the original publication dates. Add primary-source links for Berlin and Hamburg dog rules and for the federal, Berlin, and Hamburg cat rules. Replace the inaccurate cat state list with the federal local-ordinance model and current Berlin and Hamburg requirements. Add contextual paths to the Berlin, Hamburg, and Munich directory pages where relevant. Keep static metadata and schema values outside component render functions.

**Trade-offs:** Google can rewrite snippets, and both baselines have low click and impression counts, so any CTR lift must be evaluated over a complete post-release window. The breed article links to primary rules for Berlin and Hamburg but remains a practical overview rather than legal advice for every state; users are still directed to verify their exact location with the local Ordnungsamt. Year-bearing titles and legal summaries require a real review when rules change.

**Verification:** TDD RED reproduced both old snippets, missing explicit modification dates, missing official sources, missing contextual directory links, and the outdated nationwide cat-law framing. GREEN passes all 98 frontend tests across 30 files. ESLint, TypeScript, and CRLF-aware whitespace validation pass. An isolated production build completed and all 230 routes prerendered successfully. A scan of 229 content-route HTML files found exactly one description, Open Graph title, Open Graph description, and canonical link per route, no fallback markers, and the expected target titles, descriptions, and `dateModified` values. The intentionally minimal 404 page was excluded from the content-metadata requirement.

**Production rollout:** No deployment or external Google setting change was performed. After the normal release and recrawl, compare a complete 28-day window against the 67-impression breed and 23-impression cat baselines. Evaluate canonical-page clicks, CTR, and average position together because a ranking change can affect CTR independently of the snippet.

**Rollback:** Restore the former titles, descriptions, H1 text, and visible verification dates; remove the official-source and contextual directory links; restore the previous cat-law summary only if it has been independently revalidated; remove the explicit modification dates and the two focused tests. Reverting the corrected Berlin and Hamburg cat requirements is not recommended.

## 2026-07-22T12:38:22+02:00 - Correct and strengthen the Berlin emergency-vet landing page

**Context:** Search Console showed the retired `/blog/emergency-vet-berlin-english/` URL with 120 impressions, 1 click, 0.8 percent CTR, and an average position of 8.5 during 22 June through 19 July 2026. That page already redirects to the canonical `/guides/emergency-vets-berlin` guide, so improving the canonical destination is the appropriate way to consolidate and convert the existing page-one demand. Accuracy review found that the guide incorrectly labelled FU Berlin as a 24-hour clinic, published a wrong Bärenwiese phone number, omitted two other phone numbers and VetZentrum Berlin, included unsupported treatment price ranges, and told readers that several symptoms could wait until morning.

**Decision:** Lead the title, description, and H1 with the 24-hour Berlin emergency intent and retain the canonical guide URL. Replace the clinic cards with the five 24-hour small-animal providers currently listed by the Berlin Veterinary Chamber, using click-to-call numbers and visible source links. Remove FU Berlin from the 24-hour list because its official emergency page publishes separate walk-in and after-hours arrangements. Replace the page's symptom-specific waiting advice with professional telephone triage, and replace estimated treatment prices with the statutory €50 net emergency-service fee, €59.50 gross, and the two-to-four-times GOT rate. Record `2026-07-22` as the explicit modification date, show the source-check date to readers, and give the Berlin directory route a clear contextual call to action. Keep static metadata, schema, and clinic data outside the React render function.

**Trade-offs:** Google may rewrite the snippet, and a 120-impression baseline is too small to promise a CTR increase. Emergency availability, capacity, and telephone details can change, so the page tells readers to call first and links directly to the official list. The page no longer provides illustrative total treatment prices because diagnostics and treatment vary too widely for those ranges to be reliable. Year-bearing metadata now requires a genuine source review before it is advanced.

**Verification:** TDD RED first failed against the old title and page contract. GREEN passes the focused traffic test and all 99 frontend tests across 30 files. TypeScript, ESLint, CRLF-aware whitespace validation, and the new-content em-dash check pass. An isolated production build completed and all 230 routes prerendered successfully. A scan of 229 indexable content-route HTML files found no missing or duplicate titles or descriptions and confirmed the Berlin guide's exact title, description, canonical, H1, `dateModified`, and `/vets/berlin` link. Browser inspection at 390 pixels confirmed five clinic cards within the viewport, no horizontal overflow, and an in-bounds H1. No deployment was performed.

**Production rollout:** After the normal release and Google recrawl, compare a complete 28-day window against the 120-impression, 1-click, 0.8 percent CTR, position-8.5 baseline. Evaluate the canonical guide and retired URL together until Google has fully transferred the legacy signals. Recheck the Berlin Veterinary Chamber emergency list and clinic phone numbers during the same review.

**Rollback:** Restore the prior Berlin guide metadata and content from version control and remove the Berlin regression test. This would also restore the verified clinic, triage, and fee inaccuracies, so a safer rollback is to keep the corrected facts and revert only the search-copy experiment if performance declines.

## 2026-07-22T15:17:29+02:00 - Replace false Hamburg 24-hour clinic claims with the official duty service

**Context:** Search Console showed `24hr vet near me` with 87 impressions, no clicks, and an average position of 6.5 during 22 June through 19 July 2026. The Hamburg emergency guide received 82 of those impressions on its trailing-slash URL and 5 on its no-slash URL; both resolve to the existing canonical `/blog/emergency-vet-hamburg-english`. Source review found that the page's three clinics could not be represented as permanent 24/7 providers. Tierklinik Fuhlsbüttel publishes limited emergency consultations and directs after-hours callers to the Hamburg Veterinary Chamber. Tierklinik Lademannbogen publishes weekday appointment hours and telephone instructions for emergencies. The supposed Kleintierklinik im Mühlenfeld had no credible current Hamburg source. The page also contained symptom-specific advice to wait until morning, unsupported treatment-price ranges, and unverified transport claims.

**Decision:** Preserve the ranked canonical URL but lead the title, description, and H1 with the demonstrated 24-hour Hamburg intent. Replace the static clinic list with the Hamburg Veterinary Chamber's rotating duty-service workflow and make `040 434379` the primary click-to-call action. Publish the official duty periods, link the current two-week schedule, and explain that the assigned practice changes. Replace symptom-specific waiting advice with professional telephone triage, replace estimated treatment totals with the official €50 net emergency-service fee, €59.50 gross, and two-to-four-times GOT rate, and remove unsupported transport statements. Keep English call phrases and add a clear path to the regular Hamburg directory. Hoist static metadata, schema, warning signs, call details, phrases, and travel guidance outside the React render function.

**Trade-offs:** The page no longer offers a permanent clinic list because publishing an apparently convenient but stale list is unsafe. The central number and current schedule create one extra step before travel, but they route users to the practice actually on duty. The `24-Hour` title describes the search need and Hamburg's combined daytime and out-of-hours access path; the page explicitly distinguishes ordinary daytime practices from the official night, weekend, and holiday duty periods. Google may rewrite the snippet, and an 87-impression baseline is too small to promise a CTR increase.

**Verification:** TDD RED first failed against the old title and unsupported clinic contract. GREEN passes the focused traffic test and all 99 frontend tests across 30 files. TypeScript, ESLint, CRLF-aware whitespace validation, and the new-content em-dash check pass. An isolated production build completed and all 230 routes prerendered successfully. A scan of 229 indexable content-route HTML files found no missing or duplicate titles or descriptions and confirmed the Hamburg guide's exact title, description, canonical, H1, `dateModified`, duty phone, and `/vets/hamburg` link while finding none of the retired clinic claims. Browser inspection at 390 pixels confirmed no horizontal overflow, an in-bounds H1 and CTAs, and 48- to 50-pixel emergency action heights. No deployment was performed.

**Production rollout:** After the normal release and Google recrawl, compare a complete 28-day window against the query baseline of 87 impressions, no clicks, and position 6.5. Evaluate both slash variants together until Google consolidates the historical signals. Recheck the Veterinary Chamber's duty number, service periods, and fee guidance during the same review.

**Rollback:** Restore the previous Hamburg page and traffic test from version control. This would restore the false permanent-clinic claims and unsafe guidance, so the preferred rollback is limited to the title and description if the search-copy experiment performs worse while retaining the corrected duty workflow, triage, and fee facts.

## 2026-07-23T02:06:28+02:00 - Turn homepage impressions into clearer directory paths

**Context:** Search Console showed the homepage with 444 impressions, 7 clicks, 1.6 percent CTR, and an average position of 12.3 over the reviewed 28-day period. The page already ranked for the core directory topic, but its 62-character title used generic expat wording, its only H1 was visually hidden, and prominent crawlable links to the highest-demand city directories were absent. The introductory count also described every displayable record as community verified even though the dataset includes a small unverified subset.

**Decision:** Use a 50-character query-first title and a 143-character description centered on finding English-speaking vets and local practices. Make `Find an English-Speaking Vet in Germany` a visible H1. Derive the public trust count from records whose `community_status` is `Verified`, advise visitors to confirm English availability when booking, and link directly to the verification methodology. Add crawlable, 44-pixel city links for Berlin, Hamburg, Munich, Frankfurt, and Cologne between the filters and results. Hoist static metadata, verified counts, and city-link data outside the component so interactive filtering and map state do not recreate them.

**Trade-offs:** The extra H1 and city navigation increase the height of the fixed mobile header and first results area, but the directory remains scrollable and the paths expose high-value destinations without adding a separate landing-page redesign. Google can rewrite snippets, and position changes can affect CTR independently, so the copy change remains a measured hypothesis. The homepage still includes unverified listings when a visitor deliberately disables the verified-only filter, but the headline count now describes only verified records.

**Verification:** TDD RED reproduced the old title and failed the new metadata, H1, trust, and city-link contract. GREEN passes all 100 frontend tests across 30 files, TypeScript, ESLint run directly through Node, and whitespace validation. An isolated production build completed and all 230 routes prerendered successfully. A scan of 229 content pages found exactly one title, description, and canonical per route and confirmed the homepage metadata and internal links. A headless Chromium check at 390 pixels found no horizontal overflow, kept the H1 and all five city links in bounds, confirmed 44-pixel city touch targets, and reported no console errors. Temporary output was moved to Trash and preview ports 4174 and 4175 were closed. No deployment was performed.

**Production rollout:** After the normal release and recrawl, compare a complete 28-day window against the 444-impression, 7-click, 1.6 percent CTR, position-12.3 baseline. Review homepage clicks and CTR together with average position, and check whether the five city pages gain internal discovery impressions.

**Rollback:** Restore the previous homepage title, description, hidden H1, introduction, and removal of the five city links, then remove the focused homepage test. The accurate verified-only count and booking confirmation advice are trust improvements and should be retained if only the search-copy experiment is rolled back.

## 2026-07-23T02:19:20+02:00 - Replace unsupported Munich dog-transit advice with current MVV rules

**Context:** Search Console showed the retired `/blog/public-transport-with-dogs-munich-2025/` URL with 159 impressions, 4 clicks, 2.5 percent CTR, and an average position of 12.4 over the reviewed 28-day period. It already redirects to the canonical `/blog/public-transport-with-dogs-munich` route. The canonical guide used an overlong branded title and contained unsupported claims that large dogs are generally expected to wear muzzles, inspectors apply a special rush-hour rule, IsarCard has a unique dog benefit, and MVV Bergbus accepts dogs. It also hardcoded the Deutschlandticket price, described Munich as Germany's financial hub on the blog index, and omitted the MVV FLEX exception.

**Decision:** Preserve the canonical route and legacy redirects. Lead the 49-character title, description, and H1 with the demonstrated Munich dog-transit intent. Use MVV's current carriage page and 2026 joint-fare conditions as the local source of truth: one dog travels free with the passenger's valid MVV ticket or Deutschlandticket; each additional dog needs a child ticket unless carried in a basket or bag; dogs must be kept on a lead; a muzzle is required if the dog presents a risk; and dangerous dogs are excluded. Separate MVV FLEX because it generally does not accept dogs except guide dogs, assistance dogs, or dogs in suitable carriers. Explain that Deutschlandticket dog benefits are regional and use Deutsche Bahn's current rules for travel beyond MVV. Remove fixed prices and unsupported operator behavior. Add an explicit `2026-07-23` modification date, official source links, a Munich directory call to action, and corrected blog discovery copy. Hoist metadata, schemas, source URLs, FAQs, and navigation data outside the React component.

**Trade-offs:** The guide is shorter and less colorful because anecdotal claims and unverified service details were removed. It does not publish a child-ticket amount because fares and zones can change; readers are directed to current MVV information. Carrying a muzzle is presented as a practical precaution, not as a blanket official rule. Deutsche Bahn guidance does not replace the conditions of every regional operator, so the guide tells readers to check the specific connection outside MVV.

**Verification:** TDD RED reproduced the old title, metadata, unsupported advice, missing FLEX distinction, missing official sources, stale blog card, and missing modification date. GREEN passes all 102 frontend tests across 30 files, TypeScript, ESLint, whitespace validation, and the new-content em-dash check. An isolated production build completed and all 230 routes prerendered successfully. A scan of 229 content pages found exactly one title, description, and canonical per route and confirmed the Munich guide's metadata, canonical, H1, modification date, source links, and `/vets/munich` path while finding none of the retired claims. Headless Chromium at 390 pixels found no horizontal overflow, kept the H1 and article in bounds, contained the wide table in a horizontal scroller, confirmed a 48-pixel directory CTA, and reported no console errors. Temporary build output was moved to Trash and preview ports 4174 and 4175 were closed. No deployment was performed.

**Production rollout:** After the normal release and Google recrawl, compare a complete 28-day window against the 159-impression, 4-click, 2.5 percent CTR, position-12.4 baseline. Evaluate the canonical and legacy URL together until signals consolidate. Recheck the MVV carriage page, joint-fare conditions, and FLEX exception during the same review.

**Rollback:** Restore the previous Munich guide and blog discovery entry from version control and remove the Munich regression tests. That would restore unsupported and potentially misleading transit advice, so the preferred rollback is limited to the title and description if search performance declines while retaining the corrected rules and sources.

## 2026-07-23T11:39:55+02:00 - Rebuild the dog-food guide around verifiable selection criteria

**Context:** Search Console showed 116 impressions, 2 clicks, 1.7 percent CTR, and an average position of 9.9 for the retired `/blog/best-pet-food-germany-dinner-for-dogs/` URL during the reviewed 28-day period. The canonical `/blog/best-dog-food-germany` route received another 27 impressions, 1 click, 3.7 percent CTR, and an average position of 10. The legacy route already redirects to the canonical guide. Content review found an overlong title, a commercial recommendation dominating the search description, unsupported product and health claims, ingredient-order advice that conflicts with WSAVA's selection guidance, and unsafe simplification of raw diets and food transitions.

**Decision:** Preserve the canonical route and existing legacy redirects. Lead the title, description, and H1 with the demonstrated dog-food comparison intent. Base the guide on the EU distinction between `Alleinfuttermittel` and `Ergänzungsfuttermittel`, FEDIAF's current nutritional reference, and WSAVA's manufacturer and raw-diet guidance. Explain that dry, wet, and cooked foods can all be appropriate when complete and suitable, and that ingredient order or marketing terms do not establish nutritional quality. Add manufacturer quality-control questions, raw and homemade diet risks, veterinary guidance for medical diets, and an explicit `2026-07-23` modification date. Retain Zooplus and Dinner for Dogs links only in a separate commercial disclosure that states the site has not independently laboratory-tested, ranked, or nutritionally endorsed them. Update the blog discovery card to reflect the evidence-based scope. Hoist static metadata, schemas, sources, FAQs, and navigation data outside the React component.

**Trade-offs:** The article no longer provides a simple brand ranking or colorful promises about meat percentage, dental benefits, coat changes, or fresh-food outcomes because those claims were not substantiated. The guide requires readers to evaluate labels, manufacturer practices, and individual needs, which is less immediate but more defensible. FEDIAF is an industry federation, so its nutritional guidance is paired with EU law and WSAVA veterinary resources. Google can rewrite snippets, and the combined 143-impression baseline is small, so a CTR increase is a measured hypothesis rather than a promise.

**Verification:** TDD RED first reproduced the old metadata, unsupported claims, missing sources, commercial framing, stale blog card, and absent modification date. GREEN passes all 9 focused traffic tests and all 104 frontend tests across 30 files in the default concurrent run and in a serial confirmation run. Initial concurrent runs exposed a homepage test-ordering weakness: that test waited for Helmet's title and then read its description before waiting for the separate head update. The assertion now waits for both values and the default suite is green. TypeScript, ESLint, changed-file whitespace validation, and the new-content em-dash check pass. An isolated production build completed and all 230 routes prerendered. A scan of 229 content pages found exactly one title, description, and canonical per route and confirmed the dog-food guide's metadata, H1, schema date, official sources, affiliate labels, disclosure, and removal of the retired claims. Headless Chromium at 390 pixels found no horizontal overflow, kept the H1, article, sources, and commercial links in bounds, confirmed 48- and 50-pixel affiliate touch targets, and reported no console or page errors.

**Production rollout:** No deployment or external Google setting change is included. After the normal release and recrawl, compare a complete 28-day window against the combined 143-impression, 3-click, 2.1 percent CTR baseline. Evaluate the canonical and retired URL together until Google has consolidated their signals, and review CTR alongside average position.

**Rollback:** Restore the previous dog-food page and blog discovery entry from version control and remove the two dog-food regression tests. That would also restore unsupported nutrition and product claims, so the safer rollback is limited to the title and description if the search-copy experiment underperforms while retaining the corrected guidance, sources, and affiliate disclosure.

## 2026-07-23T12:19:24+02:00 - Rebuild the cat-food guide around complete diets and individual needs

**Context:** Search Console showed `/blog/best-cat-food-germany` with 51 impressions, 2 clicks, 3.9 percent CTR, and an average position of 8.8 during the reviewed 28-day period. The page already ranked between positions 6 and 10 for its clearest queries, including `best cat food in germany`, `best cat food germany`, `cat food germany`, and `best kitten food germany`. Content review found an overlong branded title, an unsupported ingredient-first quality test, a blanket recommendation of wet food as the main diet, an unsupported dry-only urinary-risk claim, and minimal source and affiliate disclosure.

**Decision:** Preserve the canonical route and lead the title, description, and H1 with the demonstrated best-cat-food search intent. Base the guide on the EU complete and complementary feed definitions, FEDIAF's 2025 cat nutrient reference, WSAVA's manufacturer-selection guidance, and Cornell Feline Health Center guidance on complete diets, wet and dry formats, and urinary emergencies. Explain that both wet and dry foods can be nutritionally complete, that wet food contributes more water, and that format alone does not establish overall quality. Explain taurine as part of the complete formulation rather than a single additive-line test. Add kitten life-stage guidance, manufacturer questions, urgent obstruction signs, an explicit `2026-07-23` modification date, and a clear directory call to action. Retain the Zooplus destination only in a separate commercial disclosure that states the site has not independently laboratory-tested, ranked, or nutritionally endorsed the products sold there. Update the blog discovery card and hoist static metadata, schemas, sources, FAQs, and navigation outside the React component.

**Trade-offs:** The article no longer gives readers a universal wet-over-dry rule or a simple meat-first shopping shortcut. It requires checking the complete-food statement, life stage, calories, manufacturer practices, water intake, and the individual cat, which is less immediate but more accurate. FEDIAF is an industry federation, so its nutrient reference is paired with EU law, WSAVA veterinary resources, and Cornell veterinary guidance. The urinary section describes emergency warning signs without claiming that one retail format prevents or treats disease. Google can rewrite snippets, and the 51-impression baseline is small, so a CTR increase remains a measured hypothesis.

**Verification:** TDD RED first reproduced the old metadata, unsupported format and ingredient claims, missing source contract, weak affiliate disclosure, stale discovery card, and absent modification date. GREEN passes all 11 focused traffic tests and all 106 frontend tests across 30 files. TypeScript, ESLint, changed-file whitespace validation, and the new-content em-dash check pass. An isolated production build completed and all 230 routes prerendered. A scan of 229 content pages found exactly one title, description, and canonical per route and confirmed the cat guide's metadata, H1, schema date, veterinary sources, affiliate label, disclosure, and removal of the retired claims. Headless Chromium at 390 pixels found no horizontal overflow, kept the H1, article, sources, vet call to action, and affiliate link in bounds, confirmed 72- and 48-pixel action heights, and reported no console or page errors.

**Production rollout:** No deployment or external Google setting change is included. After the normal release and recrawl, compare a complete 28-day window against the 51-impression, 2-click, 3.9 percent CTR, position-8.8 baseline. Review clicks and CTR alongside average position and the four core query variants.

**Rollback:** Restore the previous cat-food page and blog discovery entry from version control and remove the two cat-food regression tests. That would restore unsupported nutrition and urinary claims, so the safer rollback is limited to the title and description if the search-copy experiment underperforms while retaining the corrected guidance, sources, and disclosure.

## 2026-07-23T15:08:40+02:00 - Replace fixed tick-season and product advice with risk-based guidance

**Context:** Search Console showed `/blog/tick-season-germany-pets/` with 49 impressions, no clicks, no recorded CTR, and an average position of 6.3 during the reviewed 28-day period. Query rows were unavailable at this low volume. The existing page had page-one visibility but used an overlong branded title and unsupported claims about Germany's tick burden, nationwide season dates, temperature thresholds, regional pet-risk rankings, product strength, product combinations, and a universal Lyme transmission window. It also named and ranked commercial products without an evidence-based comparison and gave conflicting removal instructions.

**Decision:** Preserve the canonical route and lead the title, description, and H1 with the demonstrated tick-season topic and explicit dog and cat safety intent. Base the guide on ESCCAP's individual risk model, German BVL medicine-safety guidance, and RKI's stated scope for human TBE risk areas. Explain that activity varies with weather, region, habitat, travel, and the individual animal. Recommend inspection after exposure, prompt removal, only species-authorised products, and no product layering unless a veterinarian directs it. Add a prominent permethrin warning for cats, pathogen-specific transmission framing, symptom triage, an explicit `2026-07-23` modification date, FAQ schema, official sources, and a directory call to action. Update the blog discovery card and hoist metadata, schemas, sources, FAQs, and navigation outside the component.

**Trade-offs:** The guide no longer gives a simple national calendar, regional ranking, brand list, or automatic year-round medication rule. Readers need an individual veterinary plan that accounts for exposure and local conditions. The RKI map is retained only with a clear warning that it describes human TBE epidemiology, not pet tick density. Google can rewrite snippets, and 49 impressions are too few to promise a CTR increase.

**Verification:** TDD RED first reproduced the old metadata, unsafe claims, missing source contract, stale discovery card, and absent modification date. GREEN passes all 13 focused traffic tests and all 108 frontend tests across 30 files. TypeScript, ESLint, changed-file whitespace validation, and the new-content em-dash check pass. An isolated production build completed and all 230 routes prerendered. A scan of 229 content pages found exactly one title, description, and canonical per route and confirmed the tick guide's metadata, H1, schema dates, official sources, directory path, and removal of the retired claims. Headless Chromium at 390 pixels found no visible horizontal overflow, kept the H1, article, sources, and directory action in bounds, confirmed a 72-pixel action height, and reported no console or page errors.

**Production rollout:** No deployment or external Google setting change is included. After the normal release and recrawl, compare a complete 28-day window against the 49-impression, zero-click, position-6.3 baseline. Evaluate clicks and CTR alongside average position.

**Rollback:** Restore the previous tick page and blog discovery entry from version control and remove the two tick regression tests. That would restore unsupported and potentially unsafe medical guidance, so the safer rollback is limited to the title and description if the search-copy experiment underperforms while retaining the corrected safety content and sources.

## 2026-07-23T18:01:55+02:00 - Correct Cologne dog-transit fares and carriage rules

**Context:** Search Console showed `/blog/public-transport-with-dogs-cologne/` with 28 impressions, no clicks, no recorded CTR, and an average position of 5.9 during the reviewed 28-day period. Query rows were unavailable at this low volume. The page already had page-one visibility but incorrectly told readers to buy a child or short-trip ticket for a dog. It also published outdated ticket prices, treated €60 as a dog-ticket fine, overstated muzzle duties for large dogs, and presented unsupported inspector, boarding-door, route, peak-time, and destination advice as fact.

**Decision:** Preserve the canonical route and lead the title, description, and H1 with Cologne public-transport dog intent. Use KVB's current FAQ and the NRW transport conditions effective July 2026 as the carriage sources: dogs travel free; there is no separate dog ticket; a suitable person must supervise the dog on a short leash; a dog that could endanger other passengers needs a muzzle; dogs must not occupy seats; other animals need suitable containers; and assistance dogs are always admitted. Explain that animal carriage is not an automatic right and remains subject to safety, order, other passengers, and staff instructions. Use Cologne's official dog guidance to distinguish dangerous dogs and specified breeds from large dogs and explain official exemptions. Add an explicit `2026-07-23` modification date, FAQ schema, official sources, NRW boundary guidance, and a Cologne directory action. Update the blog discovery card and hoist metadata, schemas, sources, FAQs, and navigation outside the component.

**Trade-offs:** The guide no longer contains ticket prices, supposedly easier routes, fixed rush-hour windows, destination recommendations, or anecdotes about inspector behavior. Quieter travel remains optional comfort advice rather than an invented operator rule. The page distinguishes transport conditions from separate NRW and Cologne dog law, but readers with a classified dog must still confirm their individual exemption status with the public-order office. Google can rewrite snippets, and 28 impressions are too few to promise a CTR increase.

**Verification:** TDD RED first reproduced the old metadata, false ticket contract, overbroad muzzle advice, stale discovery card, missing official sources, and absent modification date. GREEN passes all 15 focused traffic tests and all 110 frontend tests across 30 files. TypeScript, ESLint, changed-file whitespace validation, and the new-content em-dash check pass. An isolated production build completed and all 230 routes prerendered. A scan of 229 content pages found exactly one title, description, and canonical per route and confirmed the Cologne guide's metadata, H1, schema dates, official sources, directory path, and removal of the retired fare and muzzle claims. Headless Chromium at 390 pixels found no visible horizontal overflow, kept the H1, article, free-travel section, sources, and directory action in bounds, confirmed a 48-pixel action height, and reported no console or page errors.

**Production rollout:** No deployment or external Google setting change is included. After the normal release and recrawl, compare a complete 28-day window against the 28-impression, zero-click, position-5.9 baseline. Evaluate clicks and CTR alongside average position.

**Rollback:** Restore the previous Cologne page and blog discovery entry from version control and remove the two Cologne regression tests. That would restore false fare and potentially misleading legal guidance, so the safer rollback is limited to the title and description if search performance declines while retaining the corrected rules and sources.

## 2026-07-23T23:09:12+02:00 - Rebuild the pet-insurance guide around policy comparison and verified rules

**Context:** Search Console showed `/blog/pet-insurance-germany` with 307 impressions, no clicks, no recorded CTR, and an average position of 33.5 during the reviewed 28-day period. Its leading queries included `pet insurance germany`, `cat insurance germany`, `insurance with dog`, `insurance for animals`, and `dog insurance germany`, all mostly ranking beyond page three. Content review found a binary recommendation to buy insurance, an outdated five-state liability summary, an overbroad description of Civil Code section 833, invented liability-loss examples, unsupported premium bands, an inaccurate one-to-four-times GOT summary, and provider rankings with unverified product and language claims.

**Decision:** Preserve the canonical route and lead the title, description, and H1 with dog and cat insurance intent. Separate dog-owner liability from dog and cat health insurance, and explain that health cover is optional while dog-liability requirements vary by federal state, dog classification, and permit situation. Use Civil Code section 833, the official GOT, Verbraucherzentrale, Berlin, and Hamburg as current sources. Explain OP-only and fuller health cover, ordinary and emergency GOT ranges, pre-existing conditions, waiting periods, age limits, deductibles, annual limits, exclusions, claim handling, renewal, and cancellation. Present insurance and a ring-fenced emergency reserve as risk choices without prescribing one universal answer. Retain three existing commercial destinations only in a separate disclosure that says they are not rankings and that policy wording, claim outcomes, pricing, exclusions, and language support were not independently audited. Add Article and FAQ schema with an explicit `2026-07-23` modification date, update the blog discovery card, and keep static metadata, schemas, navigation, FAQs, and comparison data outside the component render path.

**Trade-offs:** The guide no longer gives fixed premium estimates, illustrative loss totals, or a provider shortlist. Readers must compare current policy documents and local law, which is less immediate but avoids false precision and unstable recommendations. Berlin and Hamburg are verified examples rather than an exhaustive state matrix. The page remains informational rather than individualized legal, financial, or insurance advice. Google can rewrite snippets, and an average position of 33.5 means content coverage and authority must improve before a CTR experiment can be evaluated.

**Verification:** TDD RED reproduced both the old page contract and stale discovery card. GREEN passes the two focused tests and all 112 frontend tests across 30 files. TypeScript, ESLint, CRLF-aware whitespace validation, and the new-content em-dash check pass. An isolated production build completed and all 230 routes prerendered. A scan of 229 content pages found exactly one title, description, Open Graph title, Open Graph description, and canonical per route. The generated insurance page contains the exact metadata, schema date, official sources, three sponsored links, and none of the retired claims. Headless Chromium at 390 pixels found no global horizontal overflow, contained the 640-pixel GOT table in a 340-pixel scroller, confirmed 48-pixel commercial controls and a 72-pixel directory action, and reported no console or page errors.

**Production rollout:** No deployment or external Google setting change is included. After the normal release and recrawl, compare a complete 28-day window against the 307-impression, zero-click, position-33.5 baseline. Track the dog, cat, liability, OP-only, and general pet-insurance query groups separately because ranking movement is the primary success signal at this stage.

**Rollback:** Restore the previous insurance page and blog discovery entry from version control and remove the two insurance regression tests. That would restore unsupported legal, cost, medical-billing, and provider claims, so the safer rollback is limited to the title and description if search performance declines while retaining the corrected comparison guidance and sources.

## 2026-07-24T00:12:07+02:00 - Replace speculative veterinary prices with the statutory GOT billing model

**Context:** Search Console showed no impressions for `/blog/vet-costs-germany` during the reviewed 28-day window, so this is a trust and topical-authority correction rather than a short-term CTR experiment. The page is now linked from the higher-volume pet-insurance guide. Content review found a misleading general `1x-4x` description, unsupported city and rural billing patterns, invented all-in treatment ranges, unsafe emergency-cost avoidance advice, an absolute claim that price discussion is prohibited, a supposedly binding estimate workflow, and unsupported claims about insurance, annual checks, dental care, weight, and telemedicine savings.

**Decision:** Preserve the canonical route and explain the official calculation instead of publishing typical treatment prices. Distinguish ordinary continuous 1x to 3x billing under GOT section 2 from qualifying 2x to 4x emergency-service billing and the €50 net fee under section 4. Use the €23.62 net 1x dog, cat, and ferret examination fee only as a transparent arithmetic example and state that it is neither a typical total nor a quote. Explain separately billable services and items, the section 7 invoice requirements, the right to request itemisation, non-binding estimates, written section 5 rate agreements, payment terms, and practical ways to reduce uncertainty without delaying urgent care. Base the guide on the consolidated federal regulation and the Bundestierärztekammer owner guidance. Add an explicit `2026-07-24` modification date, update the discovery card, and hoist metadata, schemas, navigation, FAQs, invoice data, and cost-control data outside the component render path.

**Trade-offs:** The guide no longer answers “what will treatment X cost?” with a simple range because the former values had no defensible source and could not account for the selected rate, diagnostic scope, medication, materials, external services, complications, or VAT. The statutory exam illustration is precise but intentionally narrow. The page does not promise an estimate, instalment plan, or insurance reimbursement because these depend on the clinic, clinical plan, and contract. Search Console currently provides no page-level demand baseline, so success should be judged first by accuracy, internal-link usefulness, and future impressions rather than an immediate CTR lift.

**Verification:** TDD RED reproduced the old metadata, stale discovery copy, unsupported price and savings claims, missing statutory sources, absolute negotiation language, and missing modification date. GREEN passes both focused tests and all 114 frontend tests across 30 files. TypeScript, ESLint, CRLF-aware whitespace validation, and the new-content em-dash check pass. An isolated production build completed and all 230 routes prerendered. A scan of 229 content pages found exactly one title, description, Open Graph title, Open Graph description, and canonical per route. The generated vet-cost page contains the exact metadata, schema date, statutory sources, and none of the retired claims. Headless Chromium at 390 pixels found no global horizontal overflow, contained the 640-pixel fee table in a 340-pixel scroller, confirmed a 72-pixel directory action, kept the source section in bounds, and reported no console or page errors.

**Production rollout:** No deployment or external Google setting change is included. After the normal release, use the insurance guide and other relevant internal links to support discovery. Establish a Search Console baseline only after the page begins receiving impressions, then evaluate query coverage and average position before changing the snippet.

**Rollback:** Restore the previous vet-cost page and blog discovery entry from version control and remove the two regression tests. That would restore unsupported financial and emergency guidance, so the safer rollback is limited to the title and description if future search performance declines while retaining the corrected statutory billing explanation.

## 2026-07-24T00:26:35+02:00 - Route homepage authority to the veterinary cost guide

**Context:** The reviewed Search Console window showed the homepage as the site's strongest page with 340 impressions, 7 clicks, a 2.1 percent CTR, and an average position of 11.9. The rebuilt vet-cost guide had no impressions in the same window. Internal-link review found that the homepage Resource Center linked to the related insurance guide but not the vet-cost guide, while the latter depended on the low-visibility guide index and contextual links from a small set of articles.

**Decision:** Add a descriptive `Vet Costs & GOT Fees` link from the homepage Resource Center to `/blog/vet-costs-germany`. Hoist the static Resource Center data outside the Home render path and give every resource link a 44-pixel minimum tap target. Keep the desktop-only blog sidebar unchanged to avoid adding a repetitive sitewide link without evidence.

**Trade-offs:** The Resource Center grows from seven to eight entries and becomes slightly taller on mobile. This is an intentional use of homepage authority for a practical guide that supports the higher-volume insurance topic, but internal links cannot guarantee indexing or ranking gains. Avoiding the sidebar link limits raw link count while preserving stronger topical context.

**Verification:** TDD RED first proved the homepage lacked the route, and a second RED cycle captured the 28-pixel mobile tap target found during browser QA. GREEN passes the focused Home test and all 115 frontend tests across 30 files. TypeScript and ESLint pass. An isolated production build completed from the final source, generated a 229-URL sitemap, prerendered all 230 routes, and found no title, description, or canonical-count failures across 229 content pages. Headless Chrome at 390 pixels confirmed the final 44-pixel link target, no horizontal overflow on the homepage or guide, successful navigation to the correct guide title and H1, and no console or page errors. Changed-file whitespace validation and the new-content em-dash check pass. Temporary build data was moved to Trash, the local QA server was stopped, and its port was confirmed closed.

**Production rollout:** No deployment or Search Console setting change is included. After the normal release and recrawl, first watch for impressions on `/blog/vet-costs-germany`; once volume exists, evaluate query coverage and average position before testing its snippet.

**Rollback:** Remove the `Vet Costs & GOT Fees` item from `resourceLinks` and remove the corresponding Home regression test. The shared 44-pixel resource-link target can remain because it corrects an independent mobile usability issue.

## 2026-07-24T01:23:45+02:00 - Make district search pages evidence-led and status-accurate

**Context:** The three-month Search Console report showed 58 clicks, 6,804 impressions, a 0.9 percent CTR, and an average position of 14.4. Grouping all 280 visible query rows found that English-speaking vet and district searches were the largest relevant cluster: 551 impressions, one click, a 0.2 percent CTR, and an impression-weighted average position of 21. Conversational Prenzlauer Berg and Neukölln queries alone appeared repeatedly with substantial impressions. Template review found that curated district titles sometimes omitted the city, generic neighborhood copy replaced listing evidence, every result card displayed `Verified`, and unverified listings were described as definitively English-speaking.

**Decision:** For every district with active listings, build the title, description, and introduction from the current listing data instead of the legacy neighborhood-marketing map. Include the listing count, district, and city in the title. Keep the legacy copy only as a display fallback for an empty, noindex district. Define verification from exact community or verification status, use that rule consistently for filtering, display `Community Listed` for entries that are not verified, and tell visitors to confirm English availability when booking. Rewrite the shared district-content generator so unverified entries are explicitly community-sourced and English-language signals are evidence to assess rather than a guarantee.

**Trade-offs:** Titles and copy change across all 148 district routes, not only the districts already visible in Search Console. Count-led titles can change when the directory data changes, but they remain factual and keep the district and city search terms. The legacy copy remains in the source for empty noindex fallbacks, which limits this change's deletion risk but leaves a later cleanup opportunity. More cautious language is less promotional, but it avoids unsupported fluency, recommendation, and verification claims.

**Verification:** TDD RED first reproduced the missing city and count in the Prenzlauer Berg title, the universal `Verified` label in a mixed-status district, and the definitive English claim for an unverified listing. GREEN passes 12 focused tests and all 117 frontend tests across 31 files. TypeScript and ESLint pass. Headless Chrome at 390 pixels confirmed exact titles and H1s for Prenzlauer Berg and Bramfeld, zero horizontal overflow, one accurate `Verified` card, one accurate `Community Listed` card, the confirmation footer, and no console or page errors. An isolated production build generated the 229-URL sitemap, prerendered all 230 routes, found no metadata-count failures across 229 content pages, and confirmed the new title, canonical, community status, and confirmation copy in generated HTML.

**Production rollout:** No deployment or Search Console setting change is included. After the normal release and recrawl, track the English-vet cluster separately from housing, insurance, emergency, and food. For Prenzlauer Berg and Neukölln, compare clicks and CTR alongside average position because the immediate opportunity is a clearer result title and trustworthy listing evidence.

**Rollback:** Restore the previous content-selection, heading, badge, tooltip, and footer logic in `DistrictVets.tsx`; restore the previous introduction wording in `districtContent.ts`; and remove the new district SEO regression test plus the two added generator assertions. No route or canonical URL changes are involved.

## 2026-07-24T01:30:58+02:00 - Make city landing pages count-led and evidence-accurate

**Context:** The three-month Search Console review identified English-speaking vet and district searches as the largest relevant query cluster, with 551 impressions, one click, a 0.2 percent CTR, and an impression-weighted average position of 21. Every eligible district already had crawlable links from its city page, so adding more links would have been redundant. Template review instead found that curated city titles omitted the live practice count, generic copy appeared before listing-derived evidence, every card displayed `Verified`, and FAQ schema claimed every listing was confirmed and that several clinics offered round-the-clock English-speaking care regardless of the underlying city data. Hamburg currently has 33 active listings, of which 31 are Verified and two are community-listed.

**Decision:** Preserve all city routes and district links, but derive every indexed city title and description from the live listing count. Put the generated, city-specific evidence summary first and stop rendering the old unsupported promotional layer. Use exact verification status for headings, badges, tooltips, and footers. Build FAQ answers from the current verified and community-listed counts, include an emergency answer only when a listing actually records emergency or out-of-hours service, name the relevant practices, and remind readers to confirm hours and English availability. Keep the existing static city copy in source temporarily as a rollback-safe fallback map, but do not render it when active listing data exists.

**Trade-offs:** Titles will change when the listing dataset changes, but the core city search phrase and canonical URL remain stable. Replacing promotional prose with evidence-derived content is less aspirational but more trustworthy and more resistant to data drift. Keeping the retired city copy in source avoids a large deletion in this cycle, at the cost of some removable bundle text that can be cleaned up separately. The shared template affects all 38 city routes, so a regression would have broad reach.

**Verification:** TDD RED reproduced the missing count and generic Berlin lead, universal Hamburg verification labels, unsupported FAQ answers, and the stale “carefully vetted” claim. GREEN passes nine focused city-page and summary tests and all 119 frontend tests across 32 files. TypeScript, project-wide ESLint, changed-file whitespace validation, and the new-content em-dash check pass. An isolated production build generated the 229-URL sitemap, prerendered all 230 routes, and found no title, description, or canonical-count failures. The generated Berlin and Hamburg pages contain the exact count-led metadata, evidence summary, verification split, confirmation copy, and accurate FAQ count, with the retired claim absent. Headless Chrome at 390 pixels confirmed exact titles and H1s, all district links, 63 verified Berlin cards, Hamburg's 31 verified and two community-listed cards, zero page-width overflow, and no console or page errors.

**Production rollout:** No deployment or Search Console setting change is included. After the normal release and recrawl, compare city-page impressions, clicks, CTR, and average position for English-vet queries, with Berlin and Hamburg reviewed separately from district-level demand.

**Rollback:** Restore the previous city content ordering, title and description selection, FAQ answers, heading, card badge, tooltip, and footer logic in `CityVets.tsx`, then remove `CityVets.seo.test.tsx`. No route, canonical, sitemap, or dataset change is involved.

## 2026-07-24T01:49:46+02:00 - Rebuild the housing guide without resetting its search snippet

**Context:** The three-month Search Console query table contained 27 housing-related queries with 248 impressions, zero clicks, and an impression-weighted average position of 15.1. The largest terms were `pet-friendly berlin apartments` with 54 impressions, `pet friendly apartments berlin` with 39, and `pet friendly apartment guide` with 32. Historical page attribution showed the retired 2025 URL, but the latest 28-day attribution placed nearly all current impressions on the canonical route. The live trailing-slash legacy URL still takes two redirects, while the local nginx configuration already specifies a direct redirect and deployment is outside this project cycle. Content review found categorical German tenancy-law claims, an unsupported Berlin and German city ranking, invented refusal examples, unverified pet-rent and deposit advice, universal permission claims, and unsupported application-volume and termination claims.

**Decision:** Preserve the title, description, route, and canonical introduced on 22 July so the unmeasured snippet experiment remains stable. Rebuild the article around the official Bundesgerichtshof judgment VIII ZR 168/12, German Civil Code section 535, Berlin Welcome Centre apartment-search guidance, and Berlin's English rental-contract guidance. State the narrow holding accurately: a categorical dog-and-cat ban in a pre-formulated residential lease was invalid, but the judgment does not create automatic permission for every animal. Replace categorical advice with a lease-reading and written-consent workflow, a factual one-page pet resume, official Berlin search steps, a location checklist, source links, and an explicit legal-information disclaimer. Remove unsupported pet-friendly district rankings and update the blog discovery card to match the verified guide.

**Trade-offs:** The page gives fewer confident yes-or-no answers because German tenancy outcomes depend on lease wording and facts. It no longer ranks districts or promises that a pet resume, insurance certificate, or landlord reference will secure approval. The existing meta description still mentions “best districts” because it is part of the two-day-old snippet experiment; the article now answers that intent by explaining that there is no official acceptance ranking and providing a practical location checklist. Current Search Console data ends on 21 July, before the existing snippet change, so no CTR conclusion is yet possible.

**Verification:** TDD RED reproduced the missing official sources, stale discovery card, old modification date, categorical permission claims, pet-rent claim, application-volume claim, and unsupported district-acceptance ranking. GREEN passes all 20 traffic-focused tests and all 120 frontend tests across 32 files. TypeScript and project-wide ESLint pass. An isolated production build generated the 229-URL sitemap, prerendered all 230 routes, found no title, description, or canonical-count failures, and passed ten generated-page checks covering the exact metadata, canonical, modification date, official sources, legal limit, retired claims, and blog card. Headless Chrome at 390 pixels kept the article within the viewport, found all four source links, confirmed a 72-pixel directory action, found none of the retired permission, pet-rent, or district-ranking claims, and reported no console, page, or visible-overflow errors.

**Production rollout:** No deployment, redirect change, or Search Console setting change is included. After the normal release and recrawl, allow a complete 28-day measurement window before changing the title or description. Track the Berlin-specific queries separately from generic apartment-guide queries and watch whether the retired URL disappears from page attribution.

**Rollback:** Restore the previous `PetFriendlyApartments.tsx` article and the previous housing card in `Blog.tsx`, then restore the former housing assertions in `TrafficSeo.test.tsx`. That would reintroduce unsupported legal and market claims, so a safer rollback would retain the corrected body and only revert an isolated discovery-card experiment if necessary.

## 2026-07-24T02:06:47+02:00 - Rebuild the national emergency guide around local duty services

**Context:** Search Console showed `/guides/pet-emergency-germany/` with 124 impressions, no clicks, and an average position of 18 over the reviewed three-month period. The only visible queries asked how to find an emergency vet at night or on weekends and whether a same-day appointment can be booked online, but ranked at average positions 55.9 and 72.7. The page therefore had a ranking and usefulness problem rather than a snippet-only CTR problem. Content review found a blanket claim of 24/7 care in every major city, symptom-specific advice to wait until morning, an unsupported €100 to €150 initial minimum, a human poison centre labelled as pet poison control, a fixed €500 reserve recommendation, and promotional insurance claims inside the emergency workflow.

**Decision:** Preserve the canonical route but lead the title, description, and H1 with night and weekend emergency-vet intent. Explain that Germany has no single nationwide veterinary emergency number and that services are organised locally. Direct readers to their regular practice, verified Berlin and Hamburg pathways, and the official list of state veterinary chambers, with a call-before-travel requirement. Use the Federal Chamber of Veterinarians emergency guide for warning signs, remove the page's wait-until-morning list, and do not publish an unverified national animal poison hotline. Explain same-day regular appointments separately without promising online availability. Replace the cost estimate with federal GOT section 4: a €50 net fixed fee, €59.50 gross at 19 percent VAT, and two-to-four-times service rates for qualifying emergency-service care. Update the blog discovery card and keep metadata, schema, navigation, warning signs, and preparation items outside the React render path.

**Trade-offs:** The page no longer offers a simple national phone number, permanent clinic list, or total treatment estimate because those would be misleading. Readers outside Berlin and Hamburg must use their local practice or state chamber, which adds a step but follows Germany's regional duty-service structure. Google can rewrite the snippet, and the two visible queries rank too low for a metadata change alone to guarantee traffic growth. The guide now prioritises verified routing and medical safety over keyword-heavy claims.

**Verification:** TDD RED reproduced the old search contract, unsafe triage, hotline label, unsupported cost range, missing official sources, and stale blog card. GREEN passes 122 frontend tests across 32 files, 45 API tests, and 25 maintenance tests. TypeScript, ESLint, CRLF-aware whitespace validation, and the added-content em-dash check pass. An isolated production build completed and all 230 routes prerendered. A scan of 229 content pages found exactly one title, description, Open Graph title, Open Graph description, and canonical link per route. The generated emergency page contains the expected title, description, H1, canonical, source links, and `2026-07-24` modification date. Headless Chromium at 390 pixels found no horizontal overflow, kept the H1 within the viewport, measured all six key actions at 44 pixels or taller, and reported no console or page errors. Temporary build, screenshot, and Python test caches were moved to Trash, and preview port 4174 was closed.

**Test maintenance:** The full API suite exposed two stale source-shape assertions from earlier emergency-page refactors. One required a canonical URL literal to appear three times even though it is now safely hoisted into one constant; the other required the Hamburg URL in the unrelated vet-cost guide. The tests now verify the shared Berlin canonical constant and its use sites, while the Hamburg contract continues to verify its redirect, sitemap, route, blog, and national-guide references.

**Production rollout:** No deployment, Search Console validation, or live endpoint change was performed. After the normal release and recrawl, compare a complete 28-day window against the three-month baseline of 124 impressions, no clicks, and position 18. Monitor the night-and-weekend and same-day intent separately, and recheck the Federal Chamber emergency guidance, GOT section 4, and local city pathways during the same review.

**Rollback:** Restore the previous national guide and blog card, remove the two frontend regression tests, and restore the former API source-shape assertions only if the earlier component structure also returns. Restoring the wait advice, false poison-control label, blanket availability claim, or unsupported cost range is not recommended.

## 2026-07-24T02:17:22+02:00 - Rebuild the Munich emergency guide around the current LMU workflow

**Context:** Search Console reported zero clicks and zero impressions over three months for both trailing-slash variants of the Munich emergency guide. This does not support a CTR experiment. Content review instead found unsupported claims about English-speaking staff, specialist availability, trauma capability, and MRI or CT access. The guide also omitted LMU's current service limits, triage workflow, statutory emergency fees, and warning that telephone access can be limited during emergency service.

**Decision:** Preserve the canonical route and rebuild the page as a focused LMU access guide for dog and cat emergencies. State that LMU's emergency team is available 24 hours a day, provide the official address and phone number, and direct owners with a life-threatening emergency to travel immediately. Reconcile LMU's English request for short notice with its German warning about limited telephone access by recommending a call only when safe and explicitly saying not to delay urgent travel while waiting for an answer. Explain triage, emergency stabilisation, the current exclusion of dental and eye emergencies, and the statutory fee structure of €50 net, €59.50 gross at 19 percent VAT, and two-to-four-times GOT service rates. Link the official English and German LMU emergency pages, update the blog discovery card, and remove the unsupported service claims and insurance promotion.

**Trade-offs:** The page is narrower and less promotional because it describes only the emergency access and services that LMU currently documents. It does not promise English availability, specialist coverage, imaging access, a final treatment cost, telephone pickup, or instalment payment. LMU can change service limits and fees, so the guide displays its modification date and sends readers to the official pages for a final check. With no Search Console impressions, any future traffic gain must be measured from a new baseline rather than attributed to this rewrite immediately.

**Verification:** TDD RED reproduced the unsupported claims, stale metadata and discovery copy, missing official sources, missing service limits, and incomplete arrival and fee guidance. GREEN passes the two focused regression tests and all 124 frontend tests across 32 files. TypeScript, ESLint, 45 API tests, 25 maintenance tests, CRLF-aware whitespace validation, and the added-content em-dash check pass. An isolated production build completed and all 230 routes prerendered. A scan of 229 content pages found exactly one title, description, canonical, Open Graph title, and Open Graph description per route. The generated Munich page contains the exact metadata, H1, canonical, `2026-07-24` modification date, and official LMU sources, with the unsupported claims absent. Headless Chromium at 390 pixels found no horizontal overflow, kept the H1 within the viewport, measured all four critical actions at 48 pixels or taller, and reported no console or page errors.

**Production rollout:** No deployment or Search Console setting change is included. After the normal release and recrawl, establish a page-level impression baseline before testing the snippet. Recheck the official LMU English and German emergency pages during that review because operating limits and fees can change.

**Rollback:** Restore the previous `PetEmergencyMunich.tsx` page and Munich blog card, then remove the two Munich regression tests from `TrafficSeo.test.tsx`. Restoring the unsupported service claims is not recommended; if search performance later declines, retain the corrected body and test only the title or description.

## 2026-07-24T11:47:22+02:00 - Separate federal import restrictions from state dog-keeping rules

**Context:** Search Console reported 107 impressions, one click, a 0.9 percent CTR, and an average position of 10 over the reviewed three-month period for `/blog/breed-restrictions-germany/`. Google withheld the individual query rows. The report ends on 21 July 2026, while the page title and description were changed on 22 July, so the current snippet has not yet had a measurable search window. Content review found a more urgent problem: the article said there was no national breed list, treated state keeping restrictions as if they also answered the border question, and published an inaccurate nine-state table. It also claimed that affected dogs were usually grandfathered, gave an unsupported Wesenstest price, implied a temperament test broadly removed restrictions, and promoted insurance using a universal minimum-coverage claim.

**Decision:** Preserve the 22 July title, description, route, and canonical so the unmeasured snippet experiment is not reset. Rebuild the body around two distinct legal checks. First, explain the federal import and movement restriction for Pitbull Terrier, American Staffordshire Terrier, Staffordshire Bull Terrier, Bull Terrier and their crosses, together with the limited federal exceptions and German Customs guidance. Second, explain that keeping rules are set by the destination state and sometimes implemented locally. Replace the unsafe state table with sourced Berlin, Hamburg, and Bavaria examples that show materially different breed lists, permit effects, muzzle rules, and Wesenstest consequences. Add a pre-travel workflow requiring breed documentation, a federal check, a destination-authority check, and written confirmation. Remove the affiliate promotion and state clearly that a vet cannot replace Customs or the competent authority.

**Trade-offs:** The guide no longer claims to provide a complete state-by-state answer. It offers fewer destinations but each example links directly to current official rules and illustrates why a single summary table can mislead. The existing title still uses the search term “banned,” but the body carefully distinguishes a federal border prohibition from state permit and keeping regimes. Search performance cannot yet be attributed to the 22 July snippet, and Google may rewrite it. Legal classifications, forms, and authority procedures can change, so the guide requires an individual official check before travel or acquisition.

**Verification:** TDD RED reproduced the missing federal law and exceptions, stale discovery card, old modification date, unsafe generalisations, unsupported cost claim, and inaccurate Baden-Württemberg summary. GREEN passes the two focused breed-guide tests and all 125 frontend tests across 32 files. TypeScript, ESLint, 45 API tests, 25 maintenance tests, CRLF-aware whitespace validation, and the added-content em-dash check pass. An isolated production build completed and all 230 routes prerendered. A scan of 229 content pages found exactly one title, description, canonical, Open Graph title, and Open Graph description per route. The generated breed guide retains the existing title and description, contains the `2026-07-24` schema modification date and official federal, Berlin, Hamburg, and Bavaria sources, and excludes the retired claims. Headless Chromium at 390 pixels found no horizontal overflow, kept the H1 within the viewport, measured all seven checked source and directory actions at 44 pixels or taller, and reported no console or page errors.

**Production rollout:** No deployment, Search Console validation, or external setting change is included. After the normal release, preserve the current title and description until they have a complete 28-day Search Console measurement window. Compare clicks, impressions, CTR, and average position against the current 107-impression baseline, and separately recheck the official federal and destination rules before the next content update.

**Rollback:** Restore the previous body in `BreedRestrictionsGermany.tsx`, restore the former breed-restrictions Blog card, and remove the two new breed regression tests. That rollback would restore misleading legal guidance, so the safer search-only rollback is to change the Blog discovery copy while retaining the corrected federal and state distinction.

## 2026-07-24T11:59:56+02:00 - Rebuild the moving-with-pets guide around the correct 2026 entry route

**Context:** Search Console reported 50 impressions, no clicks, a 0 percent CTR, and an average position of 9.5 over three months for the legacy `/blog/moving-to-germany-with-pet-2025/` URL. The latest 28-day view reported eight impressions, no clicks, and an average position of 15.1. Google withheld the individual query rows, and the canonical `/blog/moving-to-germany-with-pet` route had no impressions over the same three-month window. The live trailing-slash legacy URL currently takes two 301 redirects to reach the canonical page. The local nginx configuration already maps both legacy variants directly to the canonical route, so the extra live hop is a pending-release issue rather than a missing local change. Content review found that the guide merged materially different travel routes, overstated quarantine and return outcomes, treated seven months as a universal minimum age, and used the retired Annex IV framing without explaining the 2026 certificate transition.

**Decision:** Preserve the canonical route and replace the guide with three explicit paths: movement from an EU country or Northern Ireland, movement from a listed non-EU country or territory, and movement from another non-EU country or territory that requires a rabies antibody titration test. Align the requirements with current European Commission and German federal guidance: identification before vaccination, the primary rabies timing rule, an EU pet passport or the applicable animal-health certificate, the listed-country test exemption, the non-listed-country blood-test timetable, the owner or authorised-person five-day rule, the five-pet limit and event exception, and entry through a designated travellers' point when required. Explain that Annex IV certificates issued before 1 October 2026 remain valid during the transition to the certificate model in Commission Delegated Regulation (EU) 2026/131. Add a separate breed-restriction check, clarify that other species use different rules, update the search title and description, and align the Blog discovery card with the revised guide.

**Trade-offs:** The guide is longer because choosing the origin route is necessary before the documents can be stated accurately. It no longer gives a universal age or timeline, a fixed endorsement-country list, or categorical quarantine and return claims. That makes the answer less compact but avoids unsafe generalisation. The canonical page has no current Search Console impressions, while the legacy URL still receives impressions, so gains depend on the live redirect configuration reaching production and Google consolidating the signals. Rules, country listings, travellers' points, and certificate formats can change, so the guide sends readers to the current official sources for final confirmation.

**Verification:** TDD RED reproduced the old title, merged route logic, stale Blog card, and outdated requirements. GREEN passes the two focused moving-guide tests and all 127 frontend tests across 32 files. TypeScript, project-wide ESLint, 45 API tests, 25 maintenance tests, CRLF-aware whitespace validation, and the added-content em-dash check pass. Five official source URLs returned HTTP 200. An isolated production build completed and all 230 routes prerendered. A scan of 229 content pages found exactly one title, description, canonical, Open Graph title, and Open Graph description per route. The generated moving guide contains the exact metadata, canonical, H1, `2026-07-24` modification date, current source links, and route-specific guidance. Headless Chromium at 390 pixels found no page-level horizontal overflow, kept the H1 and table container within the viewport, confirmed intentional table-only horizontal scrolling, measured all seven checked source and directory actions at 44 pixels or taller, and reported no console or page errors.

**Production rollout:** No deployment, redirect change, Search Console setting change, staging, or commit is included. After the normal release, verify that both legacy variants redirect to the canonical URL in one hop. Then request or observe recrawling and compare a complete 28-day Search Console window against the legacy baselines of 50 three-month impressions and eight latest-period impressions. Track whether impressions consolidate on the canonical route before drawing a CTR conclusion.

**Rollback:** Restore the previous `MovingWithPetChecklist.tsx` page and the previous moving-with-pets Blog card, then remove the two moving-guide regression tests from `TrafficSeo.test.tsx`. Restoring the merged route logic and categorical travel claims is not recommended. A safer search-only rollback would retain the corrected body and revert only the title, description, or Blog discovery copy.

## 2026-07-24T12:13:05+02:00 - Separate German dog etiquette from local dog law

**Context:** The three-month Search Console page report identified the already-reviewed cat-microchipping guide as the next page by impressions, with 55 impressions on its trailing-slash variant and 50 on its canonical variant. Its snippet and legal summary were changed on 22 July, while the available report ends on 21 July, so another change would erase the measurement window before it begins. The next genuinely unreviewed article was `/blog/german-dog-etiquette-rules/`, with 48 impressions, one click, a 2.1 percent CTR, and an average position of 10.2 over three months. Its latest 28-day view reported 17 impressions, one click, a 5.9 percent CTR, and position 8.8. Search Console disclosed only one query, `can i pet your dog in german`, with one impression and no click; the remaining query rows were withheld. The trailing-slash page redirects directly to the canonical route in one 301 hop. Content review found a false nationwide 1 April to 15 July leash period, an unsupported fine table, an overbroad Berlin leash statement, automatic restaurant and beer-garden access claims, a promised water-service custom, and social stereotypes presented as facts.

**Decision:** Preserve the canonical route but rebuild the guide around a clear separation between courtesy and law. Lead with the polite phrase `Darf ich Ihren Hund streicheln?`, explain how to ask and wait for consent, and provide additional phrases for dog greetings, distance, leashes and venue entry. Explain the four rule layers: federal welfare, federal-state law, municipal or district rules, and place or operator conditions. Use the federal Dog Welfare Ordinance for exercise and social-contact requirements, then show Berlin, Hamburg and Lower Saxony as three materially different official systems rather than a national rule table. Replace generic fine amounts with a local-authority check, state that seasonal wildlife rules vary by place, treat venue entry as a policy question, and direct transport readers to the verified Munich and Cologne guides. Add a separate breed-restriction check, update the title, description, H1, modification date and Blog discovery card, and keep metadata, schema, sources, phrases, examples and checklist data outside the React render function.

**Trade-offs:** The page no longer promises a simple national etiquette code. Readers must check the exact state, municipality, sign, operator or venue, which is less compact but legally safer. Berlin, Hamburg and Lower Saxony are examples rather than a complete sixteen-state survey. The title includes the one disclosed phrase intent, but that query produced only one impression, so the rewrite is primarily an accuracy and usefulness correction rather than a claim that one keyword will create significant traffic. Local laws, park rules, transport conditions and venue policies can change, so the guide links the current official sources and displays a verification date.

**Verification:** TDD RED reproduced the old title, stale Blog card, missing phrase-first answer, missing official sources and unsafe nationwide claims. GREEN passes the two focused dog-etiquette tests and all 129 frontend tests across 32 files. TypeScript, project-wide ESLint, 45 API tests, 25 maintenance tests, CRLF-aware whitespace validation, and the added-content em-dash check pass. The four official source URLs returned HTTP 200. An isolated production build completed and all 230 routes prerendered. A scan of 229 content pages found exactly one title, description, canonical, Open Graph title and Open Graph description per route. The generated guide contains the exact metadata, H1, canonical, `2026-07-24` modification date and eight critical source or internal links, with the retired claims absent. Headless Chromium at 390 pixels found no page-level horizontal overflow, kept the H1 and phrase-table container within the viewport, confirmed intentional table-only horizontal scrolling, measured all eight critical actions at 44 pixels or taller, found both phrase placements, and reported no console or page errors.

**Production rollout:** No deployment, Search Console setting change, staging or commit is included. After the normal release and recrawl, compare a complete 28-day window against the latest baseline of 17 impressions, one click, 5.9 percent CTR and position 8.8. Review the trailing-slash and canonical attribution together until Google consolidates the route. Leave the cat-microchipping snippet unchanged until it receives its own complete post-release measurement window.

**Rollback:** Restore the previous `GermanDogEtiquette.tsx` article and previous dog-etiquette Blog card, then remove the two focused tests from `TrafficSeo.test.tsx`. That would restore the unsupported fine, seasonal, venue and Berlin claims. A safer search-only rollback would retain the corrected guide and revert only the title, description or Blog discovery copy.

## 2026-07-24T12:40:32+02:00 - Rebuild the first-vet-visit guide as a verified appointment checklist

**Context:** The Blog index had no impressions in the latest 28-day Search Console window, so changing its snippet would not provide a measurable current experiment. The next unreviewed article, `/blog/first-vet-visit-germany/`, had four impressions, no clicks and an average position of 8 in the latest 28-day trailing-slash report. Google withheld its query rows. The trailing-slash URL redirects directly to the canonical route in one 301 hop. Content review found unsupported claims about the quality and verification status of every listed practice, appointment and English-email norms, shared waiting rooms, clinician staffing, prescribing culture, reminder systems, payment preferences, insurer billing and urgent access. It also published fixed €15 to €25 and €40 to €80 visit ranges, a generic vaccination schedule and an incomplete claim that any new vet could issue a German EU pet passport.

**Decision:** Preserve the canonical route and rebuild the page as a practical first-appointment checklist. Tell readers to inspect each directory listing status, read the quality promise and confirm English, services and access directly with the practice. Ask readers to bring a symptom timeline, vaccination and medical records, current medicines, relevant insurance information and a securely restrained pet, while checking with the practice before collecting a sample. During non-urgent decision-making, prompt questions about purpose, benefits, risks, alternatives and expected cost. Replace fixed prices with the federal GOT framework, explain ordinary one-to-three-times billing and the required invoice fields, and link the official invoice rule. Replace the generic vaccination table with the current StIKo Vet guidance. Explain that EU pet passports are travel documents issued by authorised vets for EU residents and that the travel-valid rabies vaccination follows identification by microchip. Treat payment and insurance as practice-specific and policy-specific. Keep emergencies in the separate national emergency guide. Update the Blog discovery card and hoist metadata, schema and static article data outside the React render path.

**Trade-offs:** Search demand is very small and the page has no disclosed query set, so this change cannot promise traffic growth. The new guide gives fewer universal answers because practice policies, treatment plans and insurance terms vary. It is longer than a snippet-only edit, but it removes medical, financial and operational generalisations that could mislead a reader. Official fee, travel and vaccination guidance can change, so the article displays a review date and links readers to the current sources.

**Verification:** TDD RED reproduced the stale title and discovery card before production code changed. GREEN passes the two new contracts and all 131 frontend tests across 32 files. All 45 API tests and 25 maintenance tests pass. TypeScript, project-wide ESLint, scoped whitespace validation and the added-content em-dash check pass. An isolated production build completed and prerendered all 230 routes. A scan of 229 content pages found exactly one non-empty title, description, Open Graph title, Open Graph description and canonical link per route. The generated first-visit page contains the exact metadata, H1, canonical, `2026-07-24` modification date and four official source URLs, with the retired fixed prices and universal verification claim absent. Headless Chromium at 390 pixels found no page-level horizontal overflow, kept the H1 and phrase-table container inside the viewport, confirmed intentional table-only horizontal scrolling, measured all eight checked actions at 44 pixels or taller, and reported no console or page errors.

**Production rollout:** No deployment, Search Console setting change, staging or commit is included. After the normal release and recrawl, compare a complete 28-day page-level window against the current four-impression baseline. Review the trailing-slash and canonical attribution together until Google consolidates the route. With this volume, prioritise changes in impressions and average position before interpreting CTR.

**Rollback:** Restore the previous `FirstVetVisitGermany.tsx` article and the previous first-visit Blog card, then remove the two focused tests from `TrafficSeo.test.tsx`. That would restore unsupported medical, payment, passport and access claims. A safer search-only rollback would retain the corrected body and revert only the title, description or Blog discovery copy.

## 2026-07-24T13:04:15+02:00 - Describe district practice lists with visible-only structured data

**Context:** The three-month Search Console query report showed 90 impressions at position 45.7 for `kleintierpraxis plagwitz` and 62 impressions at position 60.3 for `veterinärmediziner plagwitz`. The trailing-slash and canonical Plagwitz district URLs had 124 and 43 impressions respectively. The pending district-page rewrite already places the visible practice count and practice names in route metadata and page copy, so changing the snippet again before release and measurement would reset that experiment. The city template also had a nested practice list in its structured data, while the district template exposed only breadcrumbs and FAQs.

**Decision:** Add one `CollectionPage` JSON-LD object to each non-empty district route. Nest an `ItemList` whose positions and practice count match the unfiltered records visible on initial page load. Describe each item as `VeterinaryCare` using only the visible practice name, physical address, city, country and website link. Omit address data for mobile, home-visit or unknown-location records, and omit any absent website. Do not add hidden phone numbers, prices, ratings, reviews, opening hours or emergency claims. Keep empty district routes on `noindex` and emit no collection schema. Reuse one canonical URL value across the canonical link, Open Graph URL, breadcrumb and collection markup.

**Trade-offs:** The change creates no new routes and does not alter titles, descriptions or visible content. Structured data can help search engines understand the page, but it does not guarantee a ranking increase or rich result. District pages remain summary pages rather than individual practice profiles. Creating clinic-detail routes could address branded searches more directly, but that requires a separate content-quality, ownership and maintenance design to avoid hundreds of thin pages.

**Verification:** TDD RED reproduced the missing Plagwitz collection schema while the empty-district guard already passed. GREEN passes all four focused district tests and all 133 frontend tests across 32 files. All 45 API tests and 25 maintenance tests pass. TypeScript, project-wide ESLint and scoped whitespace validation pass. An isolated production build completed and prerendered all 230 routes. A source-to-output audit verified all 148 generated district `CollectionPage` schemas against their exact visible records, including item order, counts, names, physical-address omission rules, website presence and the absence of hidden phone, price and review fields. A separate scan found exactly one title, description and canonical on each of 229 content pages.

**Production rollout:** No deployment, Search Console setting change, staging or commit is included. After the normal release, inspect a sample of district routes with URL Inspection and validate the rendered JSON-LD. Measure the Plagwitz branded queries over a complete post-release window while combining trailing-slash and canonical attribution until redirects consolidate the signals.

**Rollback:** Remove `generateDistrictCollectionSchema`, `hasPhysicalAddress`, `collectionLd` and the collection script from `DistrictVets.tsx`, then remove the two collection tests from `DistrictVets.seo.test.tsx`. Breadcrumb and FAQ schema, visible content, route metadata and empty-route `noindex` behavior remain unchanged.

## 2026-07-24T13:44:33+02:00 - Describe the visible Blog guide collection without resetting its snippet

**Context:** Search Console showed the `/blog/` index with 18 impressions, no clicks and an average position of 8.7 over three months, but no impressions in the latest 28-day window. The higher-volume pages and query clusters had already been improved or were awaiting a complete post-release measurement window. Changing the Blog title, description or H1 would create an unmeasurable snippet experiment. The existing Blog JSON-LD described only the hub and did not connect its 36 visible guide cards to an ordered collection.

**Decision:** Preserve the Blog route, title, description, H1, canonical and visible layout. Enrich the existing module-level `Blog` JSON-LD with a nested `ItemList` derived from the same `blogPosts` array that renders the cards. Give every visible guide one ordered `ListItem` containing only its displayed title, description and canonical absolute URL. During isolated build verification, the existing route collector incorrectly treated the schema's absolute `url` property as a page because it scanned every `url:` field in `Blog.tsx`. Fix that root cause in the shared build helper by accepting only internal single-slash routes, and make both sitemap generation and prerender use the tested helper.

**Trade-offs:** Structured data can clarify the relationship between the hub and its visible guides, but it does not guarantee a rich result or ranking increase. The ItemList contains all 36 cards, which increases the prerendered head size slightly. Deriving both rendering and schema from one array prevents content drift. Restricting route discovery to internal paths rejects absolute and protocol-relative URLs, which are invalid inputs for the site's route list.

**Verification:** TDD RED first reproduced the missing Blog `ItemList`. GREEN verified exact item count, order, title, description and URL parity with the visible cards. The isolated production build then exposed the absolute-schema-URL route-discovery bug; a second RED-GREEN cycle added the internal-route extraction contract and shared helper. The final verification passes all 135 frontend tests across 32 files, 45 API tests, 25 maintenance tests, TypeScript and project-wide ESLint. An isolated production build discovered 36 Blog routes, prerendered all 230 routes, preserved the exact Blog title, description and canonical, and matched the generated 36-item schema to all 36 visible cards. The temporary builds were moved to recoverable Trash and port 4174 was confirmed closed.

**Production rollout:** No deployment, Search Console setting change, visible copy change, staging or commit is included. After the normal release, inspect `/blog` through URL Inspection and verify the rendered `Blog` and nested `ItemList` data. Measure impressions and position before interpreting CTR because the latest 28-day baseline is zero impressions.

**Rollback:** Remove `BLOG_SCHEMA`, restore the previous inline Blog JSON-LD and remove the Blog collection test. If the route extractor must also be reverted, remove `extractBlogRoutes` and restore both script-local parsers, but keep the internal single-slash restriction so absolute schema URLs cannot become prerender targets.

## 2026-07-24T14:31:23+02:00 - Organize the Blog hub around reader tasks

**Context:** The Blog hub presented all 36 guides as one flat grid and placed a large affiliate promotion before any editorial guide. At 390 pixels, the page was 17,338 pixels tall, and the promotion occupied the first content viewport after the hero. The uniform card layout made emergency help, moving guidance, paperwork and everyday topics difficult to distinguish. A content audit also found unsupported English-speaking and fixed 24-hour claims in the Berlin, Hamburg and Frankfurt emergency discovery cards. Search Console showed no Blog impressions in the latest 28-day window, so the current search title, description, canonical and H1 should remain stable.

**Decision:** Preserve the Blog route, search metadata, canonical and H1. Replace the flat grid with six task-oriented destinations: Start here, Emergency and vet care, Moving and paperwork, Health and safety, Everyday life, and New pet essentials. Feature the national emergency, moving and pet-friendly apartment guides first. Move the Figo promotion below that editorial section and label it `Sponsored partner`. Render every guide through one reusable semantic card, keep the grouping and order in module-level static data, and derive the Blog `ItemList` schema from the exact displayed order. Add a runtime invariant that fails clearly if a guide is missing or duplicated. Replace the unsupported Berlin, Hamburg and Frankfurt discovery claims with narrower wording supported by the current articles.

**Trade-offs:** The hub remains a long page because all 36 guides stay directly discoverable and indexable. Task navigation and compact cards reduce scanning cost without adding client-side filter state or hiding content. Regrouping changes the visible and structured-data order, but both now share one source of truth. The sponsor still appears on the page, but editorial guidance comes first and the commercial relationship is explicit.

**Verification:** TDD RED reproduced the missing task navigation, missing hierarchy, sponsor-first placement and stale emergency claims. GREEN passes the focused Blog and schema parity tests. Full verification passes 138 frontend tests across 33 files, 45 API tests, 25 maintenance tests, TypeScript and project-wide ESLint. An isolated production build found all 36 Blog routes and prerendered all 230 routes. Browser checks at 1,280 and 390 pixels found all 36 unique guide links in section counts of 3, 5, 7, 9, 9 and 3, no horizontal overflow, 44-pixel topic targets, correct sponsor placement and no console errors or warnings. The mobile page height fell from 17,338 to 12,536 pixels, a reduction of about 28 percent.

**Production rollout:** No deployment, Search Console change, staging or commit is included. After the normal release and recrawl, monitor the Blog page over a complete 28-day window. Use impressions and guide click-through behavior to decide whether the task labels or featured guide selection need another iteration.

**Rollback:** Restore the previous flat card grid and original promotion placement in `Blog.tsx`, remove `Blog.test.tsx`, and return the Blog schema to the original `blogPosts` order. Restoring the unsupported emergency-card claims is not recommended; those copy corrections should remain even if the layout is reverted.

## 2026-07-24T15:04:48+02:00 - Release the accumulated audit and traffic improvements

**Context:** All audit, security, accessibility, analytics, canonicalization, content, structured-data and Blog discovery changes since production commit `1419a71` remained in one local working tree. The live site still served that predecessor and the old Blog layout. The user authorized deployment of the complete accumulated release. The normal VPS pull timer deploys application code after a push to `main`, while Nginx configuration remains a separately managed production artifact.

**Decision:** Ship the verified working tree as one auditable release commit on `main`. Allow the active `esv-deploy.timer` to pull, build, prerender and activate the application. Separately back up the active EnglishSpeakingVets Nginx site configuration, apply the repository site snapshot after the repository update is present on the server, run `nginx -t`, and reload only when syntax validation passes. Keep the existing CSP in report-only mode. Do not alter any other site on the shared VPS.

**Trade-offs:** This is a large release rather than a sequence of small deployments because the changes were accumulated and validated together. A single commit preserves one tested state and one clear predecessor, but a regression would affect several subsystems at once. The manual Nginx step adds operational risk, so it is gated by an exact backup, syntax validation and targeted live smoke tests. The pull deployment can take several minutes because it installs dependencies and prerenders 230 routes.

**Verification:** The release passes 138 frontend tests across 33 files, 45 API tests, 25 maintenance tests, TypeScript, project-wide ESLint, shell syntax checks and whitespace validation. `npm audit --omit=dev` reports zero vulnerabilities. `pip-audit` reports no known vulnerabilities in either runtime or development requirements, and `pip check` reports no broken requirements. An isolated production build found all 36 Blog routes, generated the 229-URL sitemap and prerendered all 230 routes. Secret-pattern review found no new credential-bearing release files. The ignored internal CSV and processing database remain present locally and are not part of the release.

**Rollback:** The application predecessor is commit `1419a71`. If live verification fails, revert the release commit on `main` and let the VPS timer redeploy the predecessor state. Restore the timestamped Nginx backup, run `nginx -t`, and reload Nginx. Confirm the homepage, API health, canonical routes and critical redirects after either rollback action.

## 2026-07-24T15:11:30+02:00 - Give exact retired routes precedence over trailing-slash canonicalization

**Context:** The post-release live SEO smoke test showed that every retired URL without a trailing slash redirected directly to its replacement, while every slash variant first redirected to the same retired URL without the slash. Nginx executes a server-level rewrite before exact location selection, so the generic trailing-slash rewrite created an unnecessary two-hop redirect chain even though exact rules existed for both variants.

**Decision:** Express the generic trailing-slash rule as a regex location instead of a server-level rewrite. Exact retired-route locations now take precedence and redirect both URL variants directly to the replacement. All other slashed content routes still canonicalize to their no-slash form.

**Verification:** The initial live smoke test supplied the failing RED case. After the configuration-only correction, `nginx -t` passed, Nginx reloaded successfully, and the complete live SEO smoke suite passed. The suite verifies 12 canonical routes, both variants of 21 retired routes, two literal search-placeholder redirects and two real search-query passthroughs.

**Rollback:** Restore `/etc/nginx/sites-available/englishspeakinggermany.online.bak-2f8bd50-before-direct-redirect-fix`, run `nginx -t`, and reload Nginx. This restores the deployed `2f8bd50` site configuration but also restores the two-hop retired-route behavior.

## 2026-07-24T17:30:00+02:00 - Align the affiliate privacy disclosure with Awin

**Context:** The site uses live Awin affiliate links and the publisher account is active, but the privacy policy still identified Admitad as the affiliate network. That stale disclosure linked to the wrong provider and reduced transparency about the tracking that can occur after a visitor follows an affiliate link.

**Decision:** Replace the Admitad section with an Awin-specific disclosure based on Awin's current official privacy and tracking materials. Explain the commission relationship, the redirect through an Awin tracking domain, the possible use of tracking cookies and other attribution methods, the principal categories of pseudonymous click and transaction data, and the purposes of attribution, reporting, commission calculation and fraud prevention. Link directly to Awin's official privacy policy and tracking opt-out page. Avoid claiming that this site receives a visitor's name, email address or payment information through ordinary affiliate reporting, and distinguish this site's legitimate interest in financing its free content from the legal responsibilities of Awin and each advertiser for their subsequent device access and processing.

**Trade-offs:** This is a transparency correction, not individualized legal advice or a complete consent-management review. Awin and advertiser tracking implementations can differ and can change, so the policy links to their current notices rather than promising one fixed technology or legal basis. The React page remains static and adds no script, request, cookie or client-side state.

**Verification:** TDD RED confirmed that the page still named Admitad and lacked Awin's official privacy and opt-out destinations. A second RED exposed missing mobile word wrapping for long legal terms and policy URLs. GREEN passes 140 frontend tests across 34 files, including the focused disclosure and mobile-wrap contracts. All 45 API tests, 25 maintenance tests, TypeScript and project-wide ESLint pass. The production build completed and prerendered all 230 routes. Headless Chromium at 390 pixels confirmed the Awin heading and official links, no remaining Admitad text, no page-level horizontal overflow, and no console or page errors.

**Rollback:** Restore the previous section in `web-app/src/pages/Privacy.tsx` and remove `web-app/src/pages/Privacy.test.tsx`. That rollback would knowingly restore an inaccurate provider disclosure and is not recommended.

## 2026-07-24T17:57:00+02:00 - Remove the unconnected Admitad site verifier

**Context:** The repository and live HTML contained an Admitad site-verification meta tag added in December 2025. Git history showed that an Admitad disclosure and affiliate labels were introduced around the same period, but no Admitad tracking URL, script or programme-specific placement was ever committed. Current tracked affiliate links use Awin domains, while the remaining direct brand URLs contain no Admitad attribution mechanism.

**Decision:** Remove only the `verify-admitad` meta tag and its token from `web-app/index.html`. Preserve the unrelated Impact site-verification tag, the active Awin links, and historical references in this decision log. Add a source-level regression test so future builds cannot silently reintroduce the obsolete Admitad verifier or token.

**Trade-offs:** Removing the tag ends public proof of domain control for any forgotten Admitad account, but it does not delete an account, programme membership, transaction history or balance. The tag can be restored if an active account is later recovered. Historical Git commits still contain the old verifier and disclosure, which is appropriate audit history rather than a live integration.

**Verification:** TDD RED confirmed that `index.html` still published both the verifier name and token. GREEN passes 141 frontend tests across 34 files, 45 API tests, 25 maintenance tests, TypeScript and project-wide ESLint. The production build completed and prerendered all 230 routes. Generated-output scanning found no Admitad verifier, token, disclosure or tracking reference, while the separate Impact verifier and active Awin links remained present.

**Rollback:** Restore the single Admitad meta tag to `web-app/index.html`. Do so only after confirming an active Admitad account that still requires domain verification.

## 2026-07-24T22:57:43+02:00 - Build a dog-liability topic cluster and private affiliate control workbook

**Context:** The site already had a broad pet-insurance guide, a dog-tax guide, a new-pet checklist and several active affiliate placements, but no dedicated guide for the high-intent question of dog-owner liability insurance. Guide discovery data was duplicated across the Blog index, sidebar, related-post component, sitemap and prerender scripts, which made new-guide publication error-prone. The user also wanted Awin revenue and expenses tracked carefully. The available Awin merchant tab redirected to sign-in when the publisher dashboard was opened, so no current revenue total or transaction export could be verified. Newsletter capture was explicitly deferred because it would create an ongoing publishing and data-maintenance obligation.

**Decision:** Publish `/blog/dog-liability-insurance-germany` as an independent, non-ranking guide based on current primary legal and state sources. Explain the different rules in Berlin, Hamburg, Lower Saxony, North Rhine-Westphalia and Bavaria, distinguish a duty to insure from civil liability, avoid guessed premiums and provide policy, quote and incident checklists. Create one typed `GUIDE_CATALOG` as the shared source for Blog cards, sidebar discovery, related-post selection, sitemap generation and prerender route discovery. Rank related guides by shared topics with deterministic fallbacks. Add direct contextual links to the new guide from the pet-insurance, dog-tax and new-pet pages. Keep the existing 36 discovery titles and descriptions unchanged, then add the new guide as item 37. Create a seven-sheet private Awin workbook outside version control with transaction and expense inputs, validations, formulas, model checks, source logging and an affiliate-program inventory. Mark actuals as unverified and keep the model status at FAIL until a signed-in Awin export reconciles. Add `outputs/` to `.gitignore` so private financial records cannot be committed accidentally.

**Trade-offs:** Centralizing discovery data removes duplication but makes the catalogue a shared dependency of both the application and build scripts. Focused contracts now protect route extraction, catalogue uniqueness, Blog grouping and canonical emergency routes. The state comparison is deliberately not a substitute for all 16 state laws, and official rules can change. The workbook displays formula zeroes while empty, but a prominent warning states that they mean no rows are loaded rather than zero revenue. No revenue chart is included because plotting unverified zeroes would be misleading. The Awin session must be restored before actuals can be loaded.

**Verification:** TDD RED reproduced the missing guide, route, catalogue entry, contextual links and sidebar behavior before each implementation. GREEN passes 152 frontend tests across 39 files, 45 API tests and 25 maintenance tests. Project-wide ESLint and whitespace checks pass. The production build found 37 guide routes, generated a 230-URL sitemap and prerendered all 231 application routes. The generated liability page contains the expected canonical, description, Article schema and FAQ schema. Headless Chromium at 390 pixels found no page-level horizontal overflow, kept the H1 within the 20-to-370-pixel content bounds, confined the wide state table to an intentional scroll container, measured all three owner-task links at 66 pixels high and reported no console or page errors. All seven workbook sheets were rendered and inspected; formula scanning found no spreadsheet errors, while the model correctly remains FAIL because the Awin source is not verified. Temporary screenshots, workbook previews and inspection files were moved to recoverable Trash, and the local preview port was confirmed closed.

**Production rollout:** No commit, push or deployment is included. After the normal release, submit or inspect the new canonical URL in Search Console and measure impressions, clicks and query coverage over a complete 28-day window. After the next Awin login, export a defined reporting period, paste the original transaction rows, reconcile the checks and only then change the source status to VERIFIED.

**Rollback:** Remove the new route, page, tests, catalogue entry and the three contextual links. Restore the duplicated guide arrays and script-local route parsing only if necessary, although retaining the centralized catalogue is safer. Delete the private workbook if it is not wanted; it is ignored by Git and does not affect the application. Restoring newsletter capture is not part of this rollback because it was not implemented.
